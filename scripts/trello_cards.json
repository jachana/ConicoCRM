{
  "lists": [
    "Ideas",
    "Feature requests",
    "Bugs",
    "Client feedback",
    "In progress",
    "In review",
    "Friendly Beta 0.1",
    "Live Beta 0.2",
    "Live 1.0"
  ],
  "labels": {
    "Core": "blue",
    "Size:M": "yellow",
    "Roadmap": "sky",
    "Size:L": "orange",
    "DTE/SII": "green",
    "Goal: Operations": "lime",
    "Bug": "pink",
    "Goal: SaaS-ready": "lime",
    "Size:XL": "red",
    "Design System": "purple",
    "Goal: Docs": "lime",
    "Goal: UX Polish": "lime",
    "Wave 1": "red",
    "Tier A": "yellow",
    "Blocked": "black",
    "Goal: Performance": "lime",
    "Size:S": "green",
    "Wave 2": "orange"
  },
  "cards": [
    {
      "list": "In review",
      "name": "[Docs] Local development setup guide",
      "labels": [
        "Roadmap",
        "Goal: Docs",
        "Size:M"
      ],
      "desc": "**Resumen**\nGuía canónica para que un nuevo desarrollador levante el stack Conico CRM (Postgres + Redis + Backend FastAPI + Celery + Frontend Vite) en menos de 30 minutos sin intervención manual.\n\n**Cambios / Decisiones**\n- Nuevo `docs/setup-local.md` (~400 líneas) con TOC, prerrequisitos, clone+`.env`, Docker Compose, migraciones Alembic, seed, tests, día-a-día, troubleshooting y apéndice de dev sin Docker.\n- Documenta los **puertos host no estándar** del `docker-compose.yml`: db `15432`, redis `16379`, backend `18000`, frontend `15173` — punto crítico que un dev nuevo no descubre solo.\n- Aclara que `backend/entrypoint.sh` corre `alembic upgrade heads` (plural) + `seed_admin.py` + `seed_all.py` automáticamente, así que en dev con Compose **no hace falta** invocarlos manualmente.\n- Documenta los dos seeds: `backend/scripts/seed_admin.py` (admin con credenciales `admin@conico.cl / changeme123`) y `backend/scripts/seed_all.py` (lee `data_seed/*.xlsx`).\n- Cubre tests: `pytest` para backend (con nota sobre `addopts = -m \"not smoke\"` en `backend/pytest.ini`), `npm test -- --run` para frontend con flag `--run` para no quedar en watch mode.\n- Cross-link a `docs/environment-variables.md` como referencia canónica de env vars en vez de duplicar — evita drift entre dos docs que se solapan.\n- Apéndice de dev nativo (sin Docker) explica cómo apuntar `DATABASE_URL` al puerto host `15432` cuando el backend corre fuera de Compose.\n- Sin cambios de código, schema, ni dependencias — change set es 1 archivo nuevo bajo `docs/`.\n",
      "checklist": [
        "Clonar el repo en un directorio limpio y seguir docs/setup-local.md sección 1-2 (cp .env.example .env, generar SECRET_KEY, docker compose up -d --build) — verificar que los 6 servicios salen healthy en `docker compose ps`.",
        "Abrir http://localhost:15173 y http://localhost:18000/api/health desde el navegador — frontend carga, health responde 200.",
        "Login con admin@conico.cl / changeme123; verificar que el seed_all corrió revisando que hay clientes/empresas/productos en las listas.",
        "Correr `docker compose exec backend pytest` y `docker compose exec frontend npm test -- --run` — ambos suites verdes (smoke tests excluidos por default, según pytest.ini).",
        "Probar `docker compose down -v && docker compose up -d --build` y confirmar que arranca limpio sin pasos manuales adicionales (entrypoint.sh aplica migraciones y seeds).",
        "Seguir el apéndice \"Desarrollo sin Docker\" (backend nativo con `DATABASE_URL=postgresql://conico:conico@localhost:15432/conico`) y confirmar que `uvicorn app.main:app --reload --port 8000` arranca contra el Postgres del Compose.",
        "Validar el cross-link a docs/environment-variables.md (sección \"1. Clonar y configurar `.env`\") — debe abrir el doc canónico de env vars, no duplicarlo."
      ]
    }
  ]
}