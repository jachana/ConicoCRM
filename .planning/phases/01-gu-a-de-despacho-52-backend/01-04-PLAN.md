---
phase: 01-gu-a-de-despacho-52-backend
plan: 04
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - backend/app/services/pdf.py
  - backend/app/services/email.py
  - backend/app/templates/guia_despacho.html
  - backend/app/api/guias_despacho.py
autonomous: true
requirements:
  - DTE-03

must_haves:
  truths:
    - "GET /api/guias-despacho/{id}/pdf retorna application/pdf con bytes WeasyPrint"
    - "POST /api/guias-despacho/{id}/email envía SMTP al destinatario y registra email_enviado_at"
    - "Asunto email = 'Guía de Despacho N°{numero} — Conico' (D-20)"
    - "Si SMTP no configurado, POST email retorna 503 con detail 'Email no configurado'"
    - "Template guia_despacho.html muestra título 'Guía de Despacho Electrónica', motivo, destino y NV vinculada"
    - "generar_pdf_guia_despacho con guía de 20 líneas renderiza en < 5s en hardware dev (W-8)"
  artifacts:
    - path: "backend/app/services/pdf.py"
      provides: "generar_pdf_guia_despacho(guia_despacho, config)"
      contains: "def generar_pdf_guia_despacho"
    - path: "backend/app/services/email.py"
      provides: "enviar_guia_despacho(guia_despacho, pdf_bytes, destinatario)"
      contains: "def enviar_guia_despacho"
    - path: "backend/app/templates/guia_despacho.html"
      provides: "Template Jinja2 para WeasyPrint"
      contains: "Guía de Despacho Electrónica"
    - path: "backend/app/api/guias_despacho.py"
      provides: "Endpoints /pdf y /email"
      contains: "/pdf"
  key_links:
    - from: "GET /{id}/pdf"
      to: "generar_pdf_guia_despacho"
      via: "Response application/pdf"
      pattern: "media_type=\"application/pdf\""
    - from: "POST /{id}/email"
      to: "enviar_guia_despacho"
      via: "EmailNotConfiguredError → 503"
      pattern: "EmailNotConfiguredError"
---

<objective>
Implementar generación de PDF (WeasyPrint) y envío SMTP para guías de despacho. Agrega los endpoints `/pdf` y `/email` al router existente, las funciones de servicio y el template HTML.

Purpose: Cierra DTE-03. Es independiente del pipeline DTE — funciona aunque la guía no se haya enviado a Lioren todavía.

Output: Usuario puede descargar PDF y enviar email desde la API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-02-SUMMARY.md

@backend/app/services/pdf.py
@backend/app/services/email.py
@backend/app/templates/boleta.html
@backend/app/api/boletas.py
@backend/app/api/guias_despacho.py
@backend/app/models/guia_despacho.py
@backend/app/schemas/guia_despacho.py

<interfaces>
From backend/app/services/pdf.py:
```python
TEMPLATES_DIR = ...
def generar_pdf_boleta(boleta, config: dict) -> bytes:
    env = Environment(loader=FileSystemLoader(TEMPLATES_DIR))
    template = env.get_template("boleta.html")
    html_str = template.render(boleta=boleta, config=config)
    return HTML(string=html_str, base_url=TEMPLATES_DIR).write_pdf()
```

From backend/app/services/email.py:
```python
class EmailNotConfiguredError(Exception): ...
def _get_smtp_config() -> dict: ...  # raises EmailNotConfiguredError si no configurado
def enviar_boleta(boleta, pdf_bytes: bytes, destinatario: str) -> None: ...
```

From backend/app/api/boletas.py (PDF/email endpoint patterns, líneas 340-380):
```python
@router.get("/{boleta_id}/pdf")
def descargar_pdf(boleta_id: int, perms = require_permission(...)):
    pdf_bytes = generar_pdf_boleta(boleta, _config_dict(db))
    return Response(content=pdf_bytes, media_type="application/pdf", headers={...})
```

From backend/app/schemas/guia_despacho.py:
```python
class GuiaEmailBody(BaseModel):
    email: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Servicios PDF + Email + template HTML</name>
  <files>backend/app/services/pdf.py, backend/app/services/email.py, backend/app/templates/guia_despacho.html</files>
  <read_first>
    - backend/app/services/pdf.py (líneas 1-50, generar_pdf_boleta y helpers TEMPLATES_DIR)
    - backend/app/services/email.py (enviar_boleta completo, _get_smtp_config, EmailNotConfiguredError)
    - backend/app/templates/boleta.html (template a copiar)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md secciones "services/pdf.py", "services/email.py", "templates/guia_despacho.html"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-18, D-19, D-20
    - **W-7 NOTA:** IGNORAR la variable `guia` que aparece en el ejemplo `pdf.py` de RESEARCH.md y en el ejemplo `enviar_guia_despacho(guia, ...)` — usar **`guia_despacho`** como nombre de parámetro y de variable Jinja (consistente con PATTERNS.md y con el template `{{ guia_despacho.* }}`). Si el ejemplo de RESEARCH.md dice `template.render(guia=guia, ...)`, la implementación correcta es `template.render(guia_despacho=guia_despacho, ...)`.
  </read_first>
  <action>
**A) `backend/app/services/pdf.py`** — agregar función después de `generar_pdf_boleta`:

```python
def generar_pdf_guia_despacho(guia_despacho, config: dict) -> bytes:
    env = Environment(loader=FileSystemLoader(TEMPLATES_DIR))
    template = env.get_template("guia_despacho.html")
    html_str = template.render(guia_despacho=guia_despacho, config=config)
    return HTML(string=html_str, base_url=TEMPLATES_DIR).write_pdf()
```

(NO modificar `generar_pdf_boleta` ni TEMPLATES_DIR. NO usar `guia=` como nombre de keyword argument en `template.render` — el template espera `guia_despacho`.)

**B) `backend/app/services/email.py`** — agregar función después de `enviar_boleta`. Replicar el patrón exacto de `enviar_boleta` adaptando subject (D-20) y nombres de variable. La firma usa `guia_despacho` (no `guia`):

```python
def enviar_guia_despacho(guia_despacho, pdf_bytes: bytes, destinatario: str) -> None:
    cfg = _get_smtp_config()  # raises EmailNotConfiguredError si SMTP no configurado
    if not destinatario:
        raise ValueError("La guía no tiene correo de destino")

    numero_str = f"GD-{guia_despacho.folio_sii or guia_despacho.numero:05d}"
    fecha_str = guia_despacho.fecha.strftime("%d/%m/%Y") if guia_despacho.fecha else ""

    msg = MIMEMultipart()
    msg["From"] = cfg["from"]
    msg["To"] = destinatario
    msg["Subject"] = f"Guía de Despacho N°{guia_despacho.numero} — Conico"  # D-20

    body = (
        f"Adjuntamos la Guía de Despacho electrónica {numero_str} "
        f"de fecha {fecha_str} por un total de $ {int(guia_despacho.total):,}.\n\n"
        f"Saludos,\nConico"
    )
    msg.attach(MIMEText(body, "plain", "utf-8"))

    filename = f"{numero_str} {fecha_str}.pdf".strip().replace("  ", " ")
    attachment = MIMEApplication(pdf_bytes, _subtype="pdf")
    attachment.add_header("Content-Disposition", "attachment", filename=filename)
    msg.attach(attachment)

    with smtplib.SMTP(cfg["host"], cfg["port"]) as server:
        server.ehlo()
        server.starttls()
        server.login(cfg["user"], cfg["password"])
        server.sendmail(cfg["from"], destinatario, msg.as_string())
```

(Si los imports `MIMEMultipart`, `MIMEText`, `MIMEApplication`, `smtplib` ya están al tope del archivo desde `enviar_boleta`, no duplicar.)

**C) `backend/app/templates/guia_despacho.html`** — copiar `boleta.html` y adaptar:

Lee primero `boleta.html` completo y duplica el archivo. Luego aplicar estos cambios mínimos:

1. Cambiar el `<title>` y el `<h1>` principal a `Guía de Despacho Electrónica`.
2. Reemplazar todas las referencias `{{ boleta.* }}` por `{{ guia_despacho.* }}`.
3. Eliminar la sección de método de pago / monto pagado (no aplica a guías).
4. Agregar bloque de motivo de traslado y destino antes de la tabla de líneas:

```html
<div class="meta-block">
  <p><strong>Motivo de Traslado:</strong> {{ guia_despacho.motivo_traslado }}</p>
  {% if guia_despacho.direccion_destino %}
  <p><strong>Dirección destino:</strong> {{ guia_despacho.direccion_destino }}{% if guia_despacho.comuna_destino %}, {{ guia_despacho.comuna_destino }}{% endif %}</p>
  {% endif %}
  {% if guia_despacho.nota_venta_id %}
  <p><strong>Nota de Venta vinculada:</strong> N°{{ guia_despacho.nota_venta_id }}</p>
  {% endif %}
</div>
```

5. Footer: agregar (o reemplazar la línea de folio existente):

```html
<div class="footer">
  <p>Folio SII: {{ guia_despacho.folio_sii or '—' }}</p>
  <p>Track ID: {{ guia_despacho.track_id or '—' }}</p>
</div>
```

NO traducir los strings al inglés. NO modificar los estilos CSS de boleta.html (mantener look&feel). NO usar la variable `guia` en el template — solo `guia_despacho`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
from app.services.pdf import generar_pdf_guia_despacho
from app.services.email import enviar_guia_despacho, EmailNotConfiguredError
import inspect
src_email = inspect.getsource(enviar_guia_despacho)
assert 'Guía de Despacho N°' in src_email and 'Conico' in src_email
assert '_get_smtp_config' in src_email
src_pdf = inspect.getsource(generar_pdf_guia_despacho)
assert 'guia_despacho=' in src_pdf, 'template.render debe usar keyword guia_despacho='
print('OK')
" &amp;&amp; test -f backend/app/templates/guia_despacho.html &amp;&amp; grep -q "Guía de Despacho Electrónica" backend/app/templates/guia_despacho.html &amp;&amp; grep -q "guia_despacho.motivo_traslado" backend/app/templates/guia_despacho.html</automated>
    <automated>cd backend &amp;&amp; python -c "
# W-8: SLA timeout — render PDF con guía de 20 líneas en < 5s
import time
from unittest.mock import MagicMock
from app.services.pdf import generar_pdf_guia_despacho

class _L:
    def __init__(self, i):
        self.descripcion = f'Item {i}'
        self.cantidad = 1
        self.precio_unitario = 1000
        self.total = 1000

g = MagicMock()
g.numero = 1
g.folio_sii = None
g.fecha = None
g.motivo_traslado = 1
g.direccion_destino = 'Av. X 1'
g.comuna_destino = 'Santiago'
g.nota_venta_id = None
g.cliente = None
g.total_neto = 20000
g.total_iva = 3800
g.total = 23800
g.lineas = [_L(i) for i in range(20)]
g.track_id = None

t0 = time.perf_counter()
pdf = generar_pdf_guia_despacho(g, {})
elapsed = time.perf_counter() - t0
assert pdf and pdf[:5] == b'%PDF-', 'PDF inválido'
assert elapsed &lt; 5.0, f'PDF render timeout: {elapsed:.2f}s &gt;= 5s (W-8 SLA)'
print(f'OK render={elapsed:.2f}s')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def generar_pdf_guia_despacho" backend/app/services/pdf.py` retorna 1 match
    - `grep "def enviar_guia_despacho" backend/app/services/email.py` retorna 1 match
    - `grep "Guía de Despacho N°" backend/app/services/email.py` retorna ≥ 1 match (D-20)
    - `grep -E "template\.render\(guia=" backend/app/services/pdf.py` retorna 0 matches (W-7: no debe usarse `guia=` como kwarg)
    - `grep "template.render(guia_despacho=" backend/app/services/pdf.py` retorna 1 match (W-7: kwarg correcto)
    - Archivo `backend/app/templates/guia_despacho.html` existe
    - `grep "Guía de Despacho Electrónica" backend/app/templates/guia_despacho.html` retorna ≥ 1 match
    - `grep "guia_despacho.motivo_traslado" backend/app/templates/guia_despacho.html` retorna ≥ 1 match
    - `grep -E "\{\{ ?guia\." backend/app/templates/guia_despacho.html` retorna 0 matches (W-7: ninguna referencia `{{ guia. }}` — solo `{{ guia_despacho. }}`)
    - `grep -E "boleta\.tipo_dte|boleta\.metodo_pago" backend/app/templates/guia_despacho.html` retorna 0 matches
    - `generar_pdf_guia_despacho` con guía mock de 20 líneas completa renderiza en **< 5s** en hardware dev (mismo SLA que boleta — W-8). El segundo bloque verify mide y asserta el tiempo.
    - Ambos bloques python del verify imprimen `OK` (incluye `OK render=<seconds>s` del SLA).
  </acceptance_criteria>
  <done>
    Servicios PDF + Email importables; template HTML existe y referencia variable `guia_despacho`. Render de 20 líneas cumple SLA < 5s.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Endpoints /pdf y /email en api/guias_despacho.py</name>
  <files>backend/app/api/guias_despacho.py</files>
  <read_first>
    - backend/app/api/guias_despacho.py (recién creado en Plan 02)
    - backend/app/api/boletas.py (líneas 340-380, endpoints PDF y email como referencia)
    - backend/app/api/config.py o similar (para ver helper `_config_dict` si existe; sino, replicar inline)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "api/guias_despacho.py" subsecciones "PDF endpoint" y "Email endpoint"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-21
  </read_first>
  <action>
Extender `backend/app/api/guias_despacho.py` agregando 2 endpoints al final del archivo. NO duplicar el helper `_load_guia` ni `_calcular_lineas_y_totales_guia` que ya existen.

**Imports adicionales** (al bloque existente):

```python
from datetime import datetime, timezone
from fastapi.responses import Response

from app.models.system_config import SystemConfig
from app.schemas.guia_despacho import GuiaEmailBody
from app.services.email import EmailNotConfiguredError, enviar_guia_despacho as _enviar_guia_email
from app.services.pdf import generar_pdf_guia_despacho
```

(Si `datetime`/`timezone`/`Response` ya están importados, no duplicar. Si `SystemConfig` ya existe en otro path, usar el path correcto del proyecto — buscar con `grep "SystemConfig" backend/app/api/boletas.py` para confirmar.)

**Helper local** (agregar después de `_load_guia` si no existe ya):

```python
def _config_dict(db: Session) -> dict:
    return {r.key: r.value for r in db.query(SystemConfig).all()}
```

(Si `_config_dict` ya está definido en `boletas.py`, importarlo desde allí en vez de redefinir: `from app.api.boletas import _config_dict`. Verificar primero qué pattern usa el proyecto.)

**Endpoint PDF**:

```python
@router.get("/{guia_id}/pdf")
def descargar_pdf_guia(
    guia_id: int,
    perms: tuple[User, Session] = require_permission("guias_despacho", "view"),
):
    _, db = perms
    guia = _load_guia(db, guia_id)
    pdf_bytes = generar_pdf_guia_despacho(guia, _config_dict(db))
    return Response(
        content=pdf_bytes,
        media_type="application/pdf",
        headers={
            "Content-Disposition": f'inline; filename="guia-{guia.numero}.pdf"',
        },
    )
```

**Endpoint Email**:

```python
@router.post("/{guia_id}/email", response_model=GuiaDespachoOut)
def enviar_email_guia(
    guia_id: int,
    body: GuiaEmailBody,
    perms: tuple[User, Session] = require_permission("guias_despacho", "edit"),
):
    _, db = perms
    guia = _load_guia(db, guia_id)
    destino = (body.email or guia.email_envio or
               (guia.cliente.email if guia.cliente and getattr(guia.cliente, "email", None) else None))
    if not destino:
        raise HTTPException(status_code=422, detail="No hay email destino")
    pdf_bytes = generar_pdf_guia_despacho(guia, _config_dict(db))
    try:
        _enviar_guia_email(guia, pdf_bytes, destino)
    except EmailNotConfiguredError:
        raise HTTPException(status_code=503, detail="Email no configurado")
    try:
        guia.email_enviado_at = datetime.now(timezone.utc)
        db.commit()
        db.refresh(guia)
    except Exception:
        db.rollback()
        raise
    return guia
```

(Si el campo `email` no existe en el modelo `Cliente`, eliminar el fallback al cliente — solo `body.email or guia.email_envio`.)

NO añadir endpoint `/anular` — sigue diferido (D-17).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
from fastapi.testclient import TestClient
from app.main import app
client = TestClient(app)
r = client.get('/api/guias-despacho/9999/pdf')
assert r.status_code in (401, 403, 404), r.status_code
r = client.post('/api/guias-despacho/9999/email', json={'email': 'a@b.cl'})
assert r.status_code in (401, 403, 404, 422), r.status_code
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/pdf" backend/app/api/guias_despacho.py` retorna ≥ 1 match
    - `grep "/email" backend/app/api/guias_despacho.py` retorna ≥ 1 match
    - `grep 'media_type="application/pdf"' backend/app/api/guias_despacho.py` retorna 1 match
    - `grep "EmailNotConfiguredError" backend/app/api/guias_despacho.py` retorna ≥ 1 match
    - `grep "Email no configurado" backend/app/api/guias_despacho.py` retorna 1 match
    - `grep -E '"/anular"|def anular_guia' backend/app/api/guias_despacho.py` retorna 0 matches (D-17 sigue diferido)
    - El bloque python del verify imprime `OK`
  </acceptance_criteria>
  <done>
    Endpoints /pdf y /email registrados en el router; 503 cuando SMTP no configurado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /pdf, /email | RBAC requiere "view" para pdf, "edit" para email |
| API → SMTP server | Saliente; STARTTLS + auth |
| Jinja2 template render | autoescape activo (heredado de boleta.html) — XSS controlado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | PDF expone datos cliente | mitigate | RBAC `require_permission("guias_despacho", "view")` |
| T-04-02 | Tampering | XSS en `descripcion` línea | mitigate | Jinja2 autoescape (heredado de boleta.html); WeasyPrint sanitiza |
| T-04-03 | DoS | PDF render bloquea worker síncrono | mitigate | SLA `< 5s` para guía de 20 líneas (W-8); verify Task 1 mide tiempo y asserta. Documentos pequeños son patrón existente |
| T-04-04 | Tampering | Email enviado a destino arbitrario | mitigate | Body schema valida estructura; el caller (admin/subadmin/vendedor con edit) ya está autenticado |
| T-04-05 | Information Disclosure | SMTP credentials en logs | mitigate | `_get_smtp_config()` lee de env; STARTTLS impide cleartext |
| T-04-06 | DoS | SMTP timeout cuelga request | accept | Patrón existente en `enviar_boleta` (síncrono); refactor async fuera de scope M1 |
</threat_model>

<verification>
- `python -c "from app.main import app"` exit 0
- Endpoints /pdf y /email responden 401/403/404/422 sin auth (script verify Task 2)
- Template renderiza con variable `guia_despacho` (script verify Task 1)
- Render de 20 líneas en < 5s (W-8 SLA) — segundo bloque verify Task 1
</verification>

<success_criteria>
- [ ] generar_pdf_guia_despacho retorna bytes WeasyPrint válidos (validado por test Plan 05)
- [ ] enviar_guia_despacho con SMTP no configurado raises EmailNotConfiguredError (mapeado a 503 en endpoint)
- [ ] Asunto email exacto: `Guía de Despacho N°{numero} — Conico`
- [ ] Template HTML referencia variable `guia_despacho` y muestra motivo + destino
- [ ] Endpoint /email registra `email_enviado_at` con timestamp UTC al éxito
- [ ] Sigue sin existir endpoint `/anular`
- [ ] PDF render con 20 líneas completas en < 5s (W-8 SLA, mismo target que boleta)
- [ ] Sin referencias `{{ guia. }}` ni `template.render(guia=...)` (W-7: variable canónica = `guia_despacho`)
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-gu-a-de-despacho-52-backend/01-04-SUMMARY.md`.
</output>
</content>
</invoke>