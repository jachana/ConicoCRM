---
phase: 01-gu-a-de-despacho-52-backend
plan: 05
type: tdd
wave: 4
depends_on: ["01", "02", "03", "04"]
files_modified:
  - backend/tests/test_guias_despacho.py
autonomous: true
requirements:
  - DTE-01
  - DTE-02
  - DTE-03
  - DTE-04
  - DTE-06
  - DTE-07

must_haves:
  truths:
    - "test_crear_guia_basica crea guía con motivo=1 y dispara emit_dte"
    - "test_crear_guia_sin_permiso_403 — vendedor sin permiso → 403"
    - "test_motivo_traslado_invalido_422 — motivo=10 → ValidationError"
    - "test_guia_no_descuenta_stock — stock invariante (D-13)"
    - "test_emitir_guia_dispara_dte — emit_dte.delay called"
    - "test_anular_guia_via_nc_emitida — NC aceptada con guia_despacho_id → guia.estado='anulada'"
    - "test_pdf_genera — bytes WeasyPrint no vacíos"
    - "test_audit_log_diff — entries en audit_log para crear+editar+anular guía"
    - "test_delete_guia_emitida_409 — DELETE en guía con dte_estado != 'no_emitida' → 409"
    - "test_numeracion_concurrente_guias — pytest.mark.skipif sqlite (Postgres-only)"
  artifacts:
    - path: "backend/tests/test_guias_despacho.py"
      provides: "Suite de tests cubriendo D-27 + D-28"
      contains: "def test_crear_guia_basica"
  key_links:
    - from: "tests"
      to: "backend.app.api.guias_despacho"
      via: "@patch('app.api.guias_despacho.emit_dte')"
      pattern: "patch.*guias_despacho"
---

<objective>
Suite de tests pytest cubriendo todos los success criteria de Phase 1: CRUD básico, permisos, validaciones, invariante stock, pipeline DTE mock, anulación vía NC, PDF, audit log, DELETE 409, y test de concurrencia Postgres-only (skipped en SQLite).

Purpose: Cierra todos los REQ-IDs (DTE-01..04, 06, 07) con verificación automatizada. Es la red de seguridad para refactors futuros.

Output: `pytest backend/tests/test_guias_despacho.py` ejecuta todos los tests sin errores en SQLite (concurrencia skip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-01-SUMMARY.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-02-SUMMARY.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-03-SUMMARY.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-04-SUMMARY.md
@.planning/codebase/TESTING.md

@backend/tests/test_boletas.py
@backend/tests/conftest.py
@backend/app/api/guias_despacho.py
@backend/app/tasks/dte.py
@backend/app/models/guia_despacho.py
@backend/app/models/nota_credito.py
@backend/app/models/dte_emision.py

<interfaces>
From backend/tests/conftest.py:
```python
@pytest.fixture
def client(): ...  # FastAPI TestClient

@pytest.fixture
def admin_token(client): ...  # JWT for admin user

@pytest.fixture
def db(): ...  # SQLAlchemy session

@pytest.fixture
def audit_enabled(): ...  # opt-in: audit_log capture
```

From backend/tests/test_boletas.py patterns:
- `@patch("app.api.boletas.emit_dte")` → mockea Celery task
- Helper `_create_producto(client, admin_token)` para tests con stock
- `pytest.mark.skipif("sqlite" in os.environ.get("DATABASE_URL", "sqlite"), reason="...")` para Postgres-only

From backend/app/tasks/dte.py:
- `_sync_dte_estado(db, emision, estado)` — invocable directamente desde tests para simular webhook
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suite de tests CRUD + permisos + validaciones + stock + audit</name>
  <files>backend/tests/test_guias_despacho.py</files>
  <read_first>
    - backend/tests/test_boletas.py (líneas 1-100 estructura, líneas 24-44 happy path, líneas 47-68 stock test, líneas 275-310 skipif concurrency)
    - backend/tests/conftest.py (fixtures, audit_enabled líneas 41-51, registro de modelos línea 77)
    - backend/app/api/guias_despacho.py (endpoints implementados en Plan 02 y 04)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "tests/test_guias_despacho.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-27, D-28
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md sección "Tests — Patrones Verificados"
  </read_first>
  <behavior>
    Cada test debe verificar UN comportamiento específico:
    1. test_crear_guia_basica → 201, dte_estado="pendiente", motivo_traslado=1, emit_dte.delay called once
    2. test_crear_guia_sin_permiso_403 → vendedor con permiso delete=False intenta DELETE → 403
    3. test_motivo_traslado_invalido_422 → POST con motivo=10 → 422
    4. test_guia_no_descuenta_stock → stock_actual no cambia tras crear guía (D-13)
    5. test_emitir_guia_dispara_dte → emit_dte.delay llamada con int(emision.id)
    6. test_anular_guia_via_nc_emitida → tras _sync_dte_estado(emision_nc, "aceptada"), guia.estado=="anulada"
    7. test_anular_guia_via_nc_pendiente_no_anula → tras _sync_dte_estado(emision_nc, "procesando"), guia.estado=="emitida" (D-16)
    8. test_pdf_genera → GET /pdf retorna bytes con magic '%PDF-' (o llamada directa a generar_pdf_guia_despacho retorna bytes)
    9. test_audit_log_diff → tras crear+editar guía, AuditLog tiene ≥ 2 entries con model_name="GuiaDespacho"
    10. test_delete_guia_emitida_409 → guía con dte_estado="aceptada" → DELETE → 409
    11. test_numeracion_concurrente_guias → skipped en SQLite (Postgres-only)
  </behavior>
  <action>
Crear `backend/tests/test_guias_despacho.py` con la suite completa. Sigue el patrón de `test_boletas.py`:

```python
import os
import uuid
from decimal import Decimal
from unittest.mock import patch

import pytest


def _create_producto(client, admin_token, sku_suffix=None):
    sku = f"SKU-GD-{sku_suffix or uuid.uuid4().hex[:8]}"
    r = client.post(
        "/api/productos/",
        json={"nombre": "Prod Guia", "sku": sku, "precio_venta": 1190, "precio_costo": 600},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201, r.text
    return r.json()


def _crear_vendedor_token(client, admin_token):
    """Crea un vendedor (rol='vendedor') y retorna su token."""
    # Patrón de test_boletas.py si existe; si no, replicar:
    suffix = uuid.uuid4().hex[:6]
    r = client.post(
        "/api/users/",
        json={"username": f"vend_{suffix}", "password": "x12345678", "role": "vendedor", "email": f"v{suffix}@x.cl"},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code in (200, 201), r.text
    rl = client.post("/api/auth/login", json={"username": f"vend_{suffix}", "password": "x12345678"})
    assert rl.status_code == 200, rl.text
    return rl.json()["access_token"]


# ── 1. CRUD básico ──────────────────────────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_crear_guia_basica(mock_emit, client, admin_token):
    r = client.post(
        "/api/guias-despacho/",
        json={
            "motivo_traslado": 1,
            "direccion_destino": "Av. Providencia 123",
            "comuna_destino": "Providencia",
            "lineas": [{"descripcion": "Item", "cantidad": "1", "precio_unitario": "1190"}],
        },
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201, r.text
    body = r.json()
    assert body["motivo_traslado"] == 1
    assert body["dte_estado"] == "pendiente"
    assert body["numero"] >= 1
    mock_emit.delay.assert_called_once()


# ── 2. Permisos (DTE-06) ────────────────────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_vendedor_no_puede_eliminar(mock_emit, client, admin_token):
    # Crear guía como admin
    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201, r.text
    guia_id = r.json()["id"]

    vend_token = _crear_vendedor_token(client, admin_token)
    rd = client.delete(
        f"/api/guias-despacho/{guia_id}",
        headers={"Authorization": f"Bearer {vend_token}"},
    )
    assert rd.status_code == 403, rd.text


# ── 3. Validación (D-05) ────────────────────────────────────────────────────

def test_motivo_traslado_invalido_422(client, admin_token):
    r = client.post(
        "/api/guias-despacho/",
        json={
            "motivo_traslado": 10,  # fuera de 1..9
            "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}],
        },
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 422, r.text


def test_lineas_vacias_422(client, admin_token):
    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": []},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 422, r.text


# ── 4. Invariante stock (D-13 / DTE-04 / INV-04) ────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_guia_no_descuenta_stock(mock_emit, client, admin_token, db):
    from app.models.producto import Producto
    from app.models.movimiento_inventario import MovimientoInventario
    prod = _create_producto(client, admin_token)
    prod_obj = db.get(Producto, prod["id"])
    stock_antes = prod_obj.stock_actual

    r = client.post(
        "/api/guias-despacho/",
        json={
            "motivo_traslado": 1,
            "lineas": [{"producto_id": prod["id"], "descripcion": "Item", "cantidad": "3", "precio_unitario": "100"}],
        },
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201, r.text
    db.expire_all()
    stock_despues = db.get(Producto, prod["id"]).stock_actual
    assert stock_despues == stock_antes, f"Stock cambió: {stock_antes} → {stock_despues}"

    movs = db.query(MovimientoInventario).filter(
        (MovimientoInventario.referencia_tipo == "guia_despacho")
        | (MovimientoInventario.referencia_tipo == "guia")
    ).all()
    assert len(movs) == 0, f"Movimientos de guía encontrados: {movs}"


# ── 5. Pipeline DTE (DTE-02) ────────────────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_emitir_guia_dispara_dte(mock_emit, client, admin_token):
    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201
    mock_emit.delay.assert_called_once()
    args, _kwargs = mock_emit.delay.call_args
    assert isinstance(args[0], int)


# ── 6. Anulación vía NC (DTE-04 / D-16) ─────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_anular_guia_via_nc_aceptada(mock_emit, client, admin_token, db):
    """NC aceptada por SII con guia_despacho_id anula la guía."""
    from app.models.guia_despacho import GuiaDespacho
    from app.models.nota_credito import NotaCredito
    from app.models.dte_emision import DteEmision
    from app.tasks.dte import _sync_dte_estado

    # 1. Crear guía
    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201, r.text
    guia_id = r.json()["id"]
    # Forzar estado emitida (la creación deja dte_estado='pendiente')
    db.expire_all()
    guia = db.get(GuiaDespacho, guia_id)
    guia.estado = "emitida"
    guia.dte_estado = "aceptada"
    db.commit()

    # 2. Crear NC vinculada (insertar directo en DB para skipear flujo NC create)
    nc = NotaCredito(
        cliente_id=guia.cliente_id,
        guia_despacho_id=guia.id,
        razon=f"Anulación guía N°{guia.numero}",
    )
    db.add(nc)
    db.flush()

    emision_nc = DteEmision(
        tipo="061",
        nota_credito_id=nc.id,
        monto_neto=int(guia.total_neto),
        monto_iva=int(guia.total_iva),
        monto_total=int(guia.total),
    )
    db.add(emision_nc)
    db.commit()

    # 3. Simular webhook estado="aceptada"
    _sync_dte_estado(db, emision_nc, "aceptada")
    db.commit()

    # 4. Verificar
    db.expire_all()
    guia_obj = db.get(GuiaDespacho, guia_id)
    assert guia_obj.estado == "anulada", f"esperado 'anulada', obtenido '{guia_obj.estado}'"


@patch("app.api.guias_despacho.emit_dte")
def test_anular_guia_nc_pendiente_no_anula(mock_emit, client, admin_token, db):
    """NC en estado 'procesando' NO anula guía (D-16)."""
    from app.models.guia_despacho import GuiaDespacho
    from app.models.nota_credito import NotaCredito
    from app.models.dte_emision import DteEmision
    from app.tasks.dte import _sync_dte_estado

    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201
    guia_id = r.json()["id"]
    guia = db.get(GuiaDespacho, guia_id)
    guia.estado = "emitida"
    db.commit()

    nc = NotaCredito(cliente_id=guia.cliente_id, guia_despacho_id=guia.id, razon="x")
    db.add(nc); db.flush()
    emision_nc = DteEmision(tipo="061", nota_credito_id=nc.id, monto_neto=0, monto_iva=0, monto_total=0)
    db.add(emision_nc); db.commit()

    _sync_dte_estado(db, emision_nc, "procesando")
    db.commit()

    db.expire_all()
    guia_obj = db.get(GuiaDespacho, guia_id)
    assert guia_obj.estado == "emitida", f"NC procesando NO debe anular; obtenido '{guia_obj.estado}'"


# ── 7. PDF (DTE-03) ─────────────────────────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_pdf_genera(mock_emit, client, admin_token):
    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201
    guia_id = r.json()["id"]
    rp = client.get(f"/api/guias-despacho/{guia_id}/pdf", headers={"Authorization": f"Bearer {admin_token}"})
    assert rp.status_code == 200, rp.text
    assert rp.headers["content-type"].startswith("application/pdf")
    assert rp.content[:5] == b"%PDF-"


# ── 8. DELETE 409 ──────────────────────────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_delete_guia_emitida_409(mock_emit, client, admin_token, db):
    from app.models.guia_despacho import GuiaDespacho
    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201
    guia_id = r.json()["id"]
    # Forzar dte_estado != "no_emitida" — ya está en "pendiente" tras crear
    rd = client.delete(f"/api/guias-despacho/{guia_id}", headers={"Authorization": f"Bearer {admin_token}"})
    assert rd.status_code == 409, rd.text
    assert "anular" in rd.json()["detail"].lower()


# ── 9. Audit log (DTE-07) ──────────────────────────────────────────────────

@patch("app.api.guias_despacho.emit_dte")
def test_audit_log_diff(mock_emit, client, admin_token, audit_enabled):
    from app.models.audit_log import AuditLog
    from app.database import SessionLocal

    r = client.post(
        "/api/guias-despacho/",
        json={"motivo_traslado": 1, "lineas": [{"descripcion": "x", "cantidad": "1", "precio_unitario": "100"}]},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 201
    guia_id = r.json()["id"]
    re_ = client.patch(
        f"/api/guias-despacho/{guia_id}",
        json={"direccion_destino": "Calle Falsa 456"},
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert re_.status_code == 200, re_.text

    db2 = SessionLocal()
    try:
        logs = db2.query(AuditLog).filter_by(model_name="GuiaDespacho").all()
        assert len(logs) >= 2, f"esperaba ≥ 2 logs, hay {len(logs)}"
    finally:
        db2.close()


# ── 10. Concurrencia Postgres-only (D-28) ──────────────────────────────────

@pytest.mark.skipif(
    "sqlite" in os.environ.get("DATABASE_URL", "sqlite"),
    reason="row-level lock with_for_update requires Postgres",
)
@patch("app.api.guias_despacho.emit_dte")
def test_numeracion_concurrente_guias(mock_emit, client, admin_token):
    # TODO(W1-05-followup): implementar test threaded como test_boletas.py:280-310 si se decide habilitar
    # en sprint. Por ahora, marcar como esqueleto skipped en SQLite.
    pytest.skip("TODO(W1-05-followup): implementar concurrencia threaded")
```

NOTAS:
- Si `_crear_vendedor_token` no aplica al modelo de auth real, leer test_boletas.py para ver cómo se crean roles en tests; replicar.
- Si el campo `email` no existe en Cliente, omitir su uso.
- Los tests asumen que `audit_enabled` fixture existe (conftest.py:41-51 según RESEARCH.md). Si no existe, agregar el `@pytest.fixture` localmente al archivo de test.
- `test_anular_guia_via_nc_aceptada` invoca `_sync_dte_estado` directamente para evitar depender del webhook completo.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/test_guias_despacho.py -x -q --tb=short 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `backend/tests/test_guias_despacho.py` existe
    - `grep -c "^def test_" backend/tests/test_guias_despacho.py` ≥ 10 (al menos 10 funciones de test)
    - `grep "test_motivo_traslado_invalido_422" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "test_guia_no_descuenta_stock" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "test_anular_guia_via_nc_aceptada" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "test_anular_guia_nc_pendiente_no_anula" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "test_audit_log_diff" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "test_pdf_genera" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "test_delete_guia_emitida_409" backend/tests/test_guias_despacho.py` retorna 1 match
    - `grep "skipif" backend/tests/test_guias_despacho.py` retorna ≥ 1 match (concurrencia Postgres-only)
    - `grep "@patch" backend/tests/test_guias_despacho.py | grep -v "^#" | grep -c "guias_despacho.emit_dte"` ≥ 5 (mock pattern correcto)
    - `pytest tests/test_guias_despacho.py -x` retorna exit 0 (todos los tests pasan o skipped)
  </acceptance_criteria>
  <done>
    Suite de ≥ 10 tests pasa en SQLite (concurrencia skip), cubriendo todos los REQ-IDs de Phase 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → DB | SQLite local; Postgres opcional para concurrencia |
| test → app | TestClient FastAPI in-process; sin red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Tests insertan datos sin RBAC | accept | Tests usan admin_token; representa el caso esperado, no ataque |
| T-05-02 | Repudiation | Tests no verifican audit | mitigate | test_audit_log_diff verifica entries con before/after diff |
| T-05-03 | Tampering | Mock de emit_dte oculta bugs reales | accept | El pipeline real se valida en sandbox Lioren (TODO follow-up); aquí mockeamos para aislar lógica de API |
</threat_model>

<verification>
- `pytest backend/tests/test_guias_despacho.py -x -q` exit 0
- Concurrencia test queda en estado "skipped" en SQLite (no error)
- audit_log diff capturado al menos 2 veces (crear + editar)
</verification>

<success_criteria>
- [ ] ≥ 10 tests cubriendo CRUD, permisos, validación, stock, pipeline mock, NC anula, PDF, audit, DELETE 409
- [ ] test_motivo_traslado_invalido_422 verifica D-05
- [ ] test_guia_no_descuenta_stock verifica D-13
- [ ] test_anular_guia_via_nc_aceptada verifica D-16
- [ ] test_anular_guia_nc_pendiente_no_anula verifica el guard de D-16
- [ ] test_audit_log_diff verifica DTE-07 con ≥ 2 entries
- [ ] test_pdf_genera verifica magic bytes %PDF-
- [ ] Concurrencia test skipped en SQLite con marker skipif
- [ ] Suite completa exit 0 en `pytest`
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-gu-a-de-despacho-52-backend/01-05-SUMMARY.md`.

Al cerrar este plan, todos los DTE-01..04, 06, 07 quedan validados por tests automatizados. Phase 1 backend está lista para mergear y Phase 2 (frontend) puede arrancar contra los contratos estables.
</output>
