---
phase: 01-gu-a-de-despacho-52-backend
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/api/guias_despacho.py
  - backend/app/core/permissions.py
  - backend/app/services/auditoria.py
  - backend/app/main.py
autonomous: true
requirements:
  - DTE-01
  - DTE-06
  - DTE-07

must_haves:
  truths:
    - "POST /api/guias-despacho/ con motivo y líneas crea guía con numero correlativo, dte_estado='pendiente' y dispara emit_dte"
    - "GET /api/guias-despacho/ pagina con filtros estado, dte_estado, cliente_id, desde, hasta"
    - "GET /api/guias-despacho/{id} retorna detalle con líneas joinedload"
    - "PATCH /api/guias-despacho/{id} solo actualiza direccion/comuna/email_envio (sin líneas, sin motivo)"
    - "DELETE retorna 204 si dte_estado='no_emitida' o 409 si ya emitida"
    - "Vendedor puede crear/editar pero NO eliminar; admin/subadmin pueden todo (DTE-06)"
    - "Mutaciones sobre GuiaDespacho/GuiaDespachoLinea quedan en audit_log con diff (DTE-07)"
    - "Dos POST simultáneos obtienen numeros distintos (SELECT FOR UPDATE en _next_numero garantiza no-colisión)"
  artifacts:
    - path: "backend/app/api/guias_despacho.py"
      provides: "Router CRUD + helpers _next_numero + cálculo totales"
      exports: ["router", "_calcular_lineas_y_totales_guia", "_load_guia"]
    - path: "backend/app/core/permissions.py"
      provides: "Modulo guias_despacho registrado"
      contains: "guias_despacho"
    - path: "backend/app/services/auditoria.py"
      provides: "GuiaDespacho/GuiaDespachoLinea en _AUDITABLE_MODEL_NAMES"
      contains: "GuiaDespacho"
    - path: "backend/app/main.py"
      provides: "Router include_router con prefix /api/guias-despacho"
      contains: "include_router(guias_despacho.router"
  key_links:
    - from: "backend/app/api/guias_despacho.py"
      to: "backend/app/api/dte.py:_next_numero"
      via: "import _next_numero"
      pattern: "from app.api.dte import _next_numero"
    - from: "backend/app/api/guias_despacho.py"
      to: "DteEmision(tipo='052')"
      via: "crear_guia_despacho dispara emit_dte.delay"
      pattern: "emit_dte\\.delay"
    - from: "backend/app/main.py"
      to: "/api/guias-despacho"
      via: "app.include_router(guias_despacho.router, prefix=...)"
      pattern: "guias_despacho\\.router"
---

<objective>
Implementar el router CRUD `/api/guias-despacho` con permisos por rol y auditoría automática. POST crea guía y dispara `emit_dte.delay`; GET/PATCH/DELETE siguen patrón boleta. Permissions module + audit whitelist + main.py wiring quedan completos.

Purpose: Entrega los endpoints HTTP que el frontend (Phase 2) y los tests consumirán. Es el corazón del CRUD.

Output: Router completo, RBAC seeded, auditoría activa, app montando rutas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@backend/app/api/boletas.py
@backend/app/api/dte.py
@backend/app/api/deps.py
@backend/app/core/permissions.py
@backend/app/services/auditoria.py
@backend/app/main.py
@backend/app/models/guia_despacho.py
@backend/app/schemas/guia_despacho.py

<interfaces>
From backend/app/api/dte.py:25-33 (reutilizar):
```python
def _next_numero(db: Session, key: str) -> int:
    cfg = db.query(SystemConfig).filter_by(key=key).with_for_update().first()
    ...
```

From backend/app/api/deps.py:
```python
def require_permission(module: str, action: str) -> Callable[..., tuple[User, Session]]:
    """FastAPI dependency that returns (current_user, db)."""
```

From backend/app/core/permissions.py:5-46:
```python
MODULES = ["catalogo", "clientes", ..., "boletas", "ordenes_compra", ...]
_DEFAULT = {role: {module: {action: bool, ...}, ...}, ...}
```

From backend/app/services/auditoria.py:47-68:
```python
_AUDITABLE_MODEL_NAMES: set[str] = {"Boleta", "BoletaLinea", "Factura", ...}
# listener before_flush captura diff before/after automáticamente
```

From backend/app/tasks/dte.py (Plan 03 lo modifica, pero ya importable):
```python
from app.tasks.dte import emit_dte  # Celery task
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Permissions + auditoría whitelist + main.py wiring</name>
  <files>backend/app/core/permissions.py, backend/app/services/auditoria.py, backend/app/main.py</files>
  <read_first>
    - backend/app/core/permissions.py (líneas 1-80, MODULES + _DEFAULT)
    - backend/app/services/auditoria.py (líneas 30-80, _AUDITABLE_MODEL_NAMES)
    - backend/app/main.py (líneas 1-100, include_router de boletas)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "core/permissions.py", "services/auditoria.py", "main.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-22, D-25, D-30
  </read_first>
  <action>
**A) `backend/app/core/permissions.py`** — agregar `"guias_despacho"` a la lista `MODULES` justo después de `"boletas"` (D-22):

Localizar `MODULES = [...]` y modificar para que incluya `"guias_despacho"` después de `"boletas"`:

```python
MODULES = [
    "catalogo", "clientes", "proveedores", "empresas", "cotizaciones", "nota_venta",
    "facturas", "boletas", "guias_despacho",
    "ordenes_compra", "inventario", "rrhh", "dashboard", "usuarios", "tareas",
]
```

(Conservar el orden y los demás valores existentes; solo insertar el nuevo módulo en su posición.)

Luego, en el dict `_DEFAULT` (defaults por rol), agregar entradas `"guias_despacho"` siguiendo el patrón de `"boletas"`:

- Para `"subadmin"`: `"guias_despacho": {"view": True, "create": True, "edit": True, "delete": True}`
- Para `"vendedor"`: `"guias_despacho": {"view": True, "create": True, "edit": True, "delete": False}`
- Admin hereda full automáticamente por el bucle `{m: {a: True for a in ACTIONS} for m in MODULES}` — no requiere cambio explícito.

**B) `backend/app/services/auditoria.py`** — agregar dos strings al set `_AUDITABLE_MODEL_NAMES` (D-25). Localizar el set (líneas ~47-68) y agregar:

```python
"GuiaDespacho",
"GuiaDespachoLinea",
```

(Insertarlos después de `"Boleta"`, `"BoletaLinea"` para mantener orden lógico.)

Verificar que ninguno de los siguientes campos esté en `SENSITIVE_FIELDS` (deben aparecer en el diff): `motivo_traslado`, `direccion_destino`, `comuna_destino`, `estado`, `dte_estado`, `track_id`, `folio_sii`. Si alguno está en SENSITIVE_FIELDS, NO modificar SENSITIVE_FIELDS — el set debe quedar igual; solo confirmar que no están.

**C) `backend/app/main.py`** — agregar import y wiring (D-30). Localizar el bloque de imports `from app.api import ...` y agregar `guias_despacho` a la lista. Localizar la línea `app.include_router(boletas.router, ...)` y agregar inmediatamente después:

```python
app.include_router(guias_despacho.router, prefix="/api/guias-despacho", tags=["guias-despacho"])
```

NO modificar middleware ni otras rutas. NO cambiar el orden de los middlewares.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
from app.core.permissions import MODULES, _DEFAULT
assert 'guias_despacho' in MODULES, MODULES
assert _DEFAULT['vendedor']['guias_despacho'] == {'view': True, 'create': True, 'edit': True, 'delete': False}, _DEFAULT['vendedor'].get('guias_despacho')
assert _DEFAULT['subadmin']['guias_despacho']['delete'] is True
from app.services.auditoria import _AUDITABLE_MODEL_NAMES
assert 'GuiaDespacho' in _AUDITABLE_MODEL_NAMES
assert 'GuiaDespachoLinea' in _AUDITABLE_MODEL_NAMES
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"guias_despacho"' backend/app/core/permissions.py` retorna ≥ 3 matches (MODULES + 2 roles)
    - `grep '"GuiaDespacho"' backend/app/services/auditoria.py` retorna ≥ 1 match
    - `grep '"GuiaDespachoLinea"' backend/app/services/auditoria.py` retorna ≥ 1 match
    - `grep "guias_despacho.router" backend/app/main.py` retorna ≥ 1 match
    - `grep 'prefix="/api/guias-despacho"' backend/app/main.py` retorna 1 match
    - El bloque python del verify imprime `OK`
  </acceptance_criteria>
  <done>
    Permissions, auditoría y wiring listos; `from app.main import app` no rompe.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Router /api/guias-despacho — POST + GET list/detalle + PATCH + DELETE</name>
  <files>backend/app/api/guias_despacho.py</files>
  <read_first>
    - backend/app/api/boletas.py (analog completo, especialmente líneas 1-200, 250-340)
    - backend/app/api/dte.py (líneas 25-33 — _next_numero a importar)
    - backend/app/models/guia_despacho.py (recién creado en Plan 01)
    - backend/app/schemas/guia_despacho.py
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "api/guias_despacho.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-07, D-13, D-23, D-29
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md "Pitfall 8" (tipo="052")
  </read_first>
  <action>
Crear `backend/app/api/guias_despacho.py` con CRUD completo. NO incluir endpoints `/pdf` y `/email` aún — eso lo hace Plan 04 extendiendo este archivo.

```python
from datetime import date, datetime, timezone
from decimal import Decimal
from fastapi import APIRouter, HTTPException, Query, status
from sqlalchemy.orm import Session, joinedload, selectinload

from app.api.deps import require_permission
from app.api.dte import _next_numero
from app.models.dte_emision import DteEmision
from app.models.guia_despacho import GuiaDespacho, GuiaDespachoLinea
from app.models.user import User
from app.schemas.guia_despacho import (
    GuiaDespachoCreate,
    GuiaDespachoListOut,
    GuiaDespachoOut,
    GuiaDespachoUpdate,
)
from app.tasks.dte import emit_dte

router = APIRouter()


def _calcular_lineas_y_totales_guia(guia: GuiaDespacho) -> None:
    """En guía, precio_unitario es BRUTO (con IVA), igual que boleta."""
    total_neto = Decimal("0")
    total_iva = Decimal("0")
    total_bruto = Decimal("0")
    tasa = Decimal("0.19")

    for linea in guia.lineas:
        bruto_unit = linea.precio_unitario
        cantidad = linea.cantidad
        descuento = linea.descuento_pct or Decimal("0")
        bruto = bruto_unit * cantidad * (Decimal("1") - descuento / Decimal("100"))

        if linea.exenta:
            neto = bruto.quantize(Decimal("0.01"))
            iva_linea = Decimal("0")
        else:
            neto = (bruto / (Decimal("1") + tasa)).quantize(Decimal("0.01"))
            iva_linea = (bruto - neto).quantize(Decimal("0.01"))

        linea.total_neto = neto
        linea.iva = iva_linea
        linea.total_linea = (neto + iva_linea).quantize(Decimal("0.01"))
        total_neto += linea.total_neto
        total_iva += linea.iva
        total_bruto += linea.total_linea

    guia.total_neto = total_neto
    guia.total_iva = total_iva
    guia.total = total_bruto


def _load_guia(db: Session, guia_id: int) -> GuiaDespacho:
    guia = (
        db.query(GuiaDespacho)
        .options(
            joinedload(GuiaDespacho.cliente),
            joinedload(GuiaDespacho.vendedor),
            selectinload(GuiaDespacho.lineas),
        )
        .filter(GuiaDespacho.id == guia_id)
        .first()
    )
    if not guia:
        raise HTTPException(status_code=404, detail="Guía no encontrada")
    return guia


@router.post("/", response_model=GuiaDespachoOut, status_code=status.HTTP_201_CREATED)
def crear_guia_despacho(
    body: GuiaDespachoCreate,
    perms: tuple[User, Session] = require_permission("guias_despacho", "create"),
):
    current_user, db = perms
    try:
        numero = _next_numero(db, "guia_despacho_last_id")
        guia = GuiaDespacho(
            numero=numero,
            fecha=body.fecha or date.today(),
            motivo_traslado=body.motivo_traslado,
            direccion_destino=body.direccion_destino,
            comuna_destino=body.comuna_destino,
            cliente_id=body.cliente_id,
            empresa_id=body.empresa_id,
            nota_venta_id=body.nota_venta_id,
            email_envio=body.email_envio,
            vendedor_id=current_user.id,
        )
        db.add(guia)
        db.flush()

        for l in body.lineas:
            db.add(GuiaDespachoLinea(
                guia_despacho_id=guia.id,
                orden=l.orden,
                producto_id=l.producto_id,
                descripcion=l.descripcion,
                cantidad=l.cantidad,
                precio_unitario=l.precio_unitario,
                descuento_pct=l.descuento_pct,
                exenta=l.exenta,
            ))
        db.flush()
        db.refresh(guia)
        _calcular_lineas_y_totales_guia(guia)

        # Guía DTE 52 NO descuenta stock — el documento tributario asociado lo hace
        # (ver INV-04 / docs/architecture.md). Invariante intencional hasta Phase 3 (D-13).

        emision = DteEmision(
            tipo="052",  # 3 chars con cero líder (Pitfall 8)
            guia_despacho_id=guia.id,
            monto_neto=int(guia.total_neto),
            monto_iva=int(guia.total_iva),
            monto_total=int(guia.total),
        )
        db.add(emision)
        db.flush()

        guia.dte_estado = "pendiente"
        db.commit()
        db.refresh(guia)
    except Exception:
        db.rollback()
        raise

    emit_dte.delay(emision.id)
    return guia


@router.get("/", response_model=list[GuiaDespachoListOut])
def listar_guias_despacho(
    estado: str | None = Query(None),
    dte_estado: str | None = Query(None),
    cliente_id: int | None = Query(None),
    desde: date | None = Query(None),
    hasta: date | None = Query(None),
    skip: int = Query(0, ge=0),
    limit: int = Query(50, le=200),
    perms: tuple[User, Session] = require_permission("guias_despacho", "view"),
):
    _, db = perms
    q = db.query(GuiaDespacho)
    if estado:
        q = q.filter(GuiaDespacho.estado == estado)
    if dte_estado:
        q = q.filter(GuiaDespacho.dte_estado == dte_estado)
    if cliente_id:
        q = q.filter(GuiaDespacho.cliente_id == cliente_id)
    if desde:
        q = q.filter(GuiaDespacho.fecha >= desde)
    if hasta:
        q = q.filter(GuiaDespacho.fecha <= hasta)
    return q.order_by(GuiaDespacho.id.desc()).offset(skip).limit(limit).all()


@router.get("/{guia_id}", response_model=GuiaDespachoOut)
def obtener_guia(
    guia_id: int,
    perms: tuple[User, Session] = require_permission("guias_despacho", "view"),
):
    _, db = perms
    return _load_guia(db, guia_id)


@router.patch("/{guia_id}", response_model=GuiaDespachoOut)
def editar_guia(
    guia_id: int,
    body: GuiaDespachoUpdate,
    perms: tuple[User, Session] = require_permission("guias_despacho", "edit"),
):
    _, db = perms
    guia = _load_guia(db, guia_id)
    try:
        for field, value in body.model_dump(exclude_unset=True).items():
            setattr(guia, field, value)
        db.commit()
        db.refresh(guia)
    except Exception:
        db.rollback()
        raise
    return guia


@router.delete("/{guia_id}", status_code=status.HTTP_204_NO_CONTENT)
def eliminar_guia(
    guia_id: int,
    perms: tuple[User, Session] = require_permission("guias_despacho", "delete"),
):
    _, db = perms
    guia = _load_guia(db, guia_id)
    if guia.dte_estado != "no_emitida":
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail="No se puede eliminar guía emitida; usá NC para anular",
        )
    try:
        db.delete(guia)
        db.commit()
    except Exception:
        db.rollback()
        raise
```

CONSTRAINTS críticos:
- NO importar `descontar_stock_boleta` ni `revertir_stock_boleta` (D-13).
- NO crear endpoint `POST /{id}/anular` (D-17 diferido — la anulación se hace creando NC con `guia_despacho_id`).
- NO redefinir `_next_numero`; importarlo desde `app.api.dte` (D-07).
- `tipo="052"` debe ser exactamente 3 caracteres con cero líder (Pitfall 8).
- Comentario inline obligatorio en `crear_guia_despacho`: `# Guía DTE 52 NO descuenta stock ...`
  </action>
  <verify>
    <!--
      B-2 fix: el verify NO ejecuta el handler POST. Solo valida que las rutas estén
      montadas (devuelven 401/403/422 sin auth) y aplica los grep gates de invariantes.
      Esto evita depender de Celery/Redis disponibles en el entorno del executor.
      Los tests reales con mock de emit_dte.delay viven en Plan 05.
    -->
    <automated>cd backend &amp;&amp; python -c "
from fastapi.testclient import TestClient
from app.main import app
client = TestClient(app)
# Solo verificamos que las rutas están montadas y responden con códigos esperados sin auth.
# NO ejecutamos el POST real con payload válido — emit_dte.delay() requiere broker Celery
# y la suite de Plan 05 cubre el happy path con @patch('app.api.guias_despacho.emit_dte').
r = client.get('/api/guias-despacho/')
assert r.status_code in (401, 403, 422), r.status_code
r = client.post('/api/guias-despacho/', json={})  # body vacío -> 401/403/422 antes de tocar Celery
assert r.status_code in (401, 403, 422), r.status_code
print('OK routes mounted')
" &amp;&amp; grep -v '^[[:space:]]*#' backend/app/api/guias_despacho.py | grep -cE "descontar_stock|revertir_stock" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from app.api.dte import _next_numero" backend/app/api/guias_despacho.py` retorna 1 match (D-07)
    - `grep 'tipo="052"' backend/app/api/guias_despacho.py` retorna 1 match (Pitfall 8)
    - `grep "Guía DTE 52 NO descuenta stock" backend/app/api/guias_despacho.py` retorna 1 match (D-13)
    - `grep -v '^[[:space:]]*#' backend/app/api/guias_despacho.py | grep -cE "descontar_stock|revertir_stock"` retorna 0 (D-13 — gate filtra comentarios para evitar self-invalidating grep)
    - `grep -E '"/anular"|def anular_guia' backend/app/api/guias_despacho.py` retorna 0 matches (D-17 diferido)
    - `grep -c '@router\\.' backend/app/api/guias_despacho.py` ≥ 5 (POST /, GET /, GET /{id}, PATCH /{id}, DELETE /{id})
    - `grep "No se puede eliminar guía emitida" backend/app/api/guias_despacho.py` retorna 1 match
    - El bloque python del verify imprime `OK routes mounted` SIN ejecutar el handler POST real (no requiere Celery/Redis).
  </acceptance_criteria>
  <done>
    Router con 5 endpoints CRUD compilable, RBAC aplicado, sin stock logic, sin endpoint anular dedicado. Verify NO depende de broker Celery.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/guias-despacho/* | JWT Bearer + RBAC se evalúan en cada request |
| sesión → audit_log | Listener SA before_flush registra mutaciones automáticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing/IDOR | GET/PATCH/DELETE /{id} | mitigate | `require_permission("guias_despacho", action)` valida usuario+rol; cualquiera con view puede leer cualquier guía (multi-tenancy fuera de scope M1) |
| T-02-02 | Elevation | DELETE por vendedor | mitigate | `_DEFAULT["vendedor"]["guias_despacho"]["delete"] = False` (D-22) → 403 |
| T-02-03 | Tampering | DELETE de guía emitida | mitigate | Guard explícito `if dte_estado != "no_emitida": 409` |
| T-02-04 | Repudiation | Mutación sin auditoría | mitigate | `_AUDITABLE_MODEL_NAMES` += {GuiaDespacho, GuiaDespachoLinea} (D-25); listener captura before/after diff vía AuditContextMiddleware |
| T-02-05 | Tampering | PATCH cambia totales/líneas | mitigate | `GuiaDespachoUpdate` solo expone metadata (direccion, comuna, email_envio) |
| T-02-06 | Information Disclosure | Audit log captura campos sensibles | accept | `track_id`, `folio_sii` se persisten porque son informaciónSII legítima de auditar (D-26) |
| T-02-07 | Tampering | Race en numeración correlativa | mitigate | `_next_numero(db, "guia_despacho_last_id")` usa `with_for_update()`; truth must_haves cubre no-colisión, test Postgres-only en Plan 05 valida bajo concurrencia real |
</threat_model>

<verification>
- App importable: `python -c "from app.main import app"` exit 0
- Rutas montadas (script verify Task 2)
- Permissions seed correcto (script verify Task 1)
- Audit whitelist contiene los modelos (script verify Task 1)
</verification>

<success_criteria>
- [ ] 5 endpoints CRUD funcionan a nivel de wiring (no auth devuelve 401/403, payload inválido 422)
- [ ] Vendedor sin permiso delete recibe 403
- [ ] Admin/subadmin pueden delete (acceptance Plan 05 lo prueba con tests reales)
- [ ] DELETE en guía emitida retorna 409
- [ ] PATCH solo modifica direccion/comuna/email
- [ ] Mutación de Guía aparece en audit_log con diff
- [ ] No existe endpoint /anular dedicado
- [ ] Sin imports de `boleta_stock` en `guias_despacho.py`
- [ ] Numeración correlativa garantizada por SELECT FOR UPDATE (truth must_haves)
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-gu-a-de-despacho-52-backend/01-02-SUMMARY.md`.
</output>
