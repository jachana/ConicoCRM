---
phase: 01-gu-a-de-despacho-52-backend
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/api/dte.py
  - backend/app/services/dte_service.py
  - backend/app/tasks/dte.py
autonomous: true
requirements:
  - DTE-01
  - DTE-02
  - DTE-04

must_haves:
  truths:
    - "POST /api/dte/guias-despacho/{id}/emitir crea DteEmision(tipo='052') y dispara emit_dte.delay"
    - "DteService.build_guia_payload retorna dict con tipo_dte=52, ind_traslado=motivo, destino={direccion,comuna}"
    - "_process_emit en tasks/dte.py reconoce emision.guia_despacho_id y construye payload via build_guia_payload"
    - "_sync_dte_estado actualiza guia.dte_estado SIN tocar stock"
    - "_sync_dte_estado en branch nota_credito_id, si estado=='aceptada' y nc.guia_despacho_id, marca guia.estado='anulada' (D-16)"
  artifacts:
    - path: "backend/app/api/dte.py"
      provides: "Endpoint emitir_guia_despacho"
      contains: "def emitir_guia_despacho"
    - path: "backend/app/services/dte_service.py"
      provides: "Método DteService.build_guia_payload"
      contains: "def build_guia_payload"
    - path: "backend/app/tasks/dte.py"
      provides: "Branch elif emision.guia_despacho_id en _process_emit y _sync_dte_estado, branch NC->guia anulada"
      contains: "elif emision.guia_despacho_id"
  key_links:
    - from: "_process_emit"
      to: "build_guia_payload"
      via: "elif guia_despacho_id branch"
      pattern: "build_guia_payload"
    - from: "_sync_dte_estado nc branch"
      to: "guia.estado='anulada'"
      via: "if estado=='aceptada' and nc.guia_despacho_id"
      pattern: "estado = \"anulada\""
    - from: "POST /api/dte/guias-despacho/{id}/emitir"
      to: "Celery emit_dte.delay"
      via: "synchronous Lioren"
      pattern: "emit_dte\\.delay"
---

<objective>
Conectar GuiaDespacho con el pipeline DTE Lioren existente: endpoint de emisión, `build_guia_payload` en DteService, y dos branches en tasks/dte.py (`_process_emit` para construir payload, `_sync_dte_estado` para actualizar estado y anular guía cuando NC vinculada se acepta).

Purpose: Cierra DTE-02 (envío SII) y DTE-04 (anulación legal por NC aceptada). Sin esto, las guías quedan en estado 'pendiente' eterno.

Output: Pipeline polimórfico extendido para tipo "052"; NC con `guia_despacho_id` aceptada anula la guía.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-01-SUMMARY.md
@.planning/codebase/INTEGRATIONS.md

@backend/app/api/dte.py
@backend/app/services/dte_service.py
@backend/app/tasks/dte.py
@backend/app/models/guia_despacho.py
@backend/app/models/nota_credito.py
@backend/app/models/dte_emision.py

<interfaces>
From backend/app/services/dte_service.py (build_boleta_payload pattern, líneas ~144-195):
```python
class DteService:
    def build_boleta_payload(self, boleta, db: Session) -> dict:
        cfg = _get_config(db)
        receptor = {...}
        detalle = [{...} for l in boleta.lineas]
        return {"tipo_dte": int(boleta.tipo_dte), "fecha_emision": ..., "emisor": ..., "receptor": ..., "detalle": ..., "totales": ...}
```

From backend/app/tasks/dte.py existing branches:
```python
def _process_emit(self, emision: DteEmision, db: Session) -> None:
    if emision.factura_id: ...
    elif emision.nota_credito_id: ...
    elif emision.nota_debito_id: ...
    elif emision.boleta_id: ...
    # ADD: elif emision.guia_despacho_id: ...

def _sync_dte_estado(db, emision, estado):
    if emision.factura_id: ...
    elif emision.nota_credito_id:
        nc = db.get(NotaCredito, emision.nota_credito_id)
        if nc: nc.dte_estado = estado
    elif emision.boleta_id:
        # ... revertir_stock_boleta si rechazada — DO NOT COPY THIS
    # ADD: elif emision.guia_despacho_id: SOLO actualiza dte_estado, sin stock
```

From backend/app/api/dte.py (emitir_factura como template, líneas 78-104):
```python
@router.post("/facturas/{factura_id}/emitir", response_model=DteEmisionOut)
def emitir_factura(factura_id: int, perms: tuple[User, Session] = require_permission("facturas", "create")):
    # validate, create DteEmision, emit_dte.delay
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: build_guia_payload en DteService</name>
  <files>backend/app/services/dte_service.py</files>
  <read_first>
    - backend/app/services/dte_service.py (build_boleta_payload completo, líneas ~144-195; _get_config, _emisor)
    - backend/app/models/guia_despacho.py
    - backend/app/models/cliente.py (campos rut, nombre, direccion_despacho, comuna)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "services/dte_service.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md sección "Lioren v1 Payload — DTE 52" y "Pitfall 4"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-11
  </read_first>
  <action>
Agregar import al tope del archivo (junto a otros imports de modelos):

```python
from app.models.guia_despacho import GuiaDespacho
```

Y agregar el siguiente método dentro de `class DteService:` (justo después de `build_boleta_payload`):

```python
def build_guia_payload(self, guia: GuiaDespacho, db: Session) -> dict:
    """Construye el payload Lioren para Guía de Despacho DTE 52.

    Field names ind_traslado / destino son hipótesis (LOW confidence — A2/A3 en RESEARCH.md).
    TODO(W1-05-sandbox): validar contra Lioren sandbox antes de merge a producción.
    Si Lioren retorna 422 por campos faltantes (Res. 154 SII: RUTChofer, Patente, FchSalida),
    extender modelo GuiaDespacho con esos campos opcionales (A4).
    """
    cfg = _get_config(db)
    receptor: dict = {}
    if guia.cliente:
        receptor = {
            "rut": guia.cliente.rut or "",
            "razon_social": guia.cliente.nombre,
            "giro": "",
            "direccion": getattr(guia.cliente, "direccion_despacho", "") or "",
            "ciudad": getattr(guia.cliente, "comuna", "") or "",
            "comuna": getattr(guia.cliente, "comuna", "") or "",
        }

    detalle = [
        {
            "nombre": l.descripcion,
            "cantidad": float(l.cantidad),
            "precio_unitario": int(l.precio_unitario),  # bruto, igual que boleta (A1)
            "descuento_porcentaje": float(l.descuento_pct or 0),
            "exenta": bool(l.exenta),
        }
        for l in guia.lineas
    ]

    payload = {
        "tipo_dte": 52,
        "fecha_emision": (guia.fecha or date.today()).isoformat(),
        "emisor": self._emisor(cfg),
        "receptor": receptor,
        "detalle": detalle,
        "totales": {
            "monto_neto": int(guia.total_neto),
            "tasa_iva": 19,
            "iva": int(guia.total_iva),
            "monto_total": int(guia.total),
        },
        # Campos específicos DTE 52 — nombres [ASSUMED, validar sandbox Lioren]:
        "ind_traslado": guia.motivo_traslado,  # 1..9
        "destino": {
            "direccion": guia.direccion_destino or "",
            "comuna": guia.comuna_destino or "",
        },
    }
    return payload
```

NO agregar `referencias: [{tipo: "PATENTE"}]` (no está en el modelo). NO agregar campos de chofer/patente — si Lioren los exige, se gestionará en follow-up post-sandbox.

Si `_get_config` o `_emisor` no existen en el archivo, NO crearlos — usar los helpers reales del archivo (ver `build_boleta_payload` para ver qué helpers reusa).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
from app.services.dte_service import DteService
import inspect
src = inspect.getsource(DteService.build_guia_payload)
assert 'tipo_dte' in src and ('52' in src or 'guia.motivo_traslado' in src)
assert 'ind_traslado' in src
assert 'destino' in src
assert 'TODO(W1-05-sandbox)' in src or 'sandbox' in src.lower()
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def build_guia_payload" backend/app/services/dte_service.py` retorna 1 match
    - `grep '"tipo_dte": 52' backend/app/services/dte_service.py` retorna 1 match
    - `grep '"ind_traslado"' backend/app/services/dte_service.py` retorna 1 match
    - `grep "destino" backend/app/services/dte_service.py | grep -c "direccion_destino\\|comuna_destino"` ≥ 2
    - `grep -i "sandbox" backend/app/services/dte_service.py` retorna ≥ 1 match (TODO marker presente)
    - El bloque python del verify imprime `OK`
  </acceptance_criteria>
  <done>
    `DteService.build_guia_payload(guia, db)` retorna dict con tipo_dte=52, motivo y destino.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Endpoint emitir_guia_despacho en api/dte.py</name>
  <files>backend/app/api/dte.py</files>
  <read_first>
    - backend/app/api/dte.py (emitir_factura líneas 78-104, emitir_nc líneas 153-179, emitir_boleta si existe)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "api/dte.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-10, D-24, D-31
  </read_first>
  <action>
Agregar import al bloque de imports existentes (junto a otros models):

```python
from app.models.guia_despacho import GuiaDespacho
```

Localizar la última sección de emisión (probablemente `# ── Notas de Débito ──`) y agregar antes del webhook handler:

```python
# ── Guías de Despacho ─────────────────────────────────────────────────────────

@router.post("/guias-despacho/{guia_id}/emitir", response_model=DteEmisionOut)
def emitir_guia_despacho(
    guia_id: int,
    perms: tuple[User, Session] = require_permission("guias_despacho", "create"),
):
    _, db = perms
    guia = db.query(GuiaDespacho).filter_by(id=guia_id).first()
    if not guia:
        raise HTTPException(status_code=404, detail="Guía no encontrada")
    if guia.dte_estado != "no_emitida":
        raise HTTPException(
            status_code=409,
            detail=f"Guía ya en estado DTE: {guia.dte_estado}",
        )
    existing = db.query(DteEmision).filter_by(guia_despacho_id=guia_id).first()
    if existing:
        raise HTTPException(status_code=409, detail="Ya existe una emisión para esta guía")
    try:
        emision = DteEmision(
            tipo="052",
            guia_despacho_id=guia.id,
            monto_neto=int(guia.total_neto),
            monto_iva=int(guia.total_iva),
            monto_total=int(guia.total),
        )
        db.add(emision)
        guia.dte_estado = "pendiente"
        db.commit()
        db.refresh(emision)
    except Exception:
        db.rollback()
        raise
    emit_dte.delay(emision.id)
    return emision
```

NOTA: Este endpoint coexiste con `crear_guia_despacho` (Plan 02) que ya dispara emit_dte. `emitir_guia_despacho` aquí permite re-emitir si `crear_guia_despacho` no lo hizo (caso edge: guía creada con dte_estado='no_emitida'). Es la primitiva paralela a `emitir_factura` / `emitir_nc` (D-31).

Permission `"guias_despacho"`,`"create"` (D-24).

Si `DteEmisionOut` no está importado, agregar al import existente desde schemas/dte.py.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
from fastapi.testclient import TestClient
from app.main import app
from app.api.dte import emitir_guia_despacho  # debe importar sin error
client = TestClient(app)
r = client.post('/api/dte/guias-despacho/9999/emitir')
assert r.status_code in (401, 403, 404, 422), r.status_code
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def emitir_guia_despacho" backend/app/api/dte.py` retorna 1 match
    - `grep '"/guias-despacho/{guia_id}/emitir"' backend/app/api/dte.py` retorna 1 match
    - `grep 'tipo="052"' backend/app/api/dte.py` retorna ≥ 1 match
    - `grep 'require_permission("guias_despacho", "create")' backend/app/api/dte.py` retorna ≥ 1 match
    - El bloque python del verify imprime `OK`
  </acceptance_criteria>
  <done>
    POST /api/dte/guias-despacho/{id}/emitir registrado, devuelve 404 sin auth o sin guía existente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Branches en tasks/dte.py (_process_emit, _sync_dte_estado, NC anula guía)</name>
  <files>backend/app/tasks/dte.py</files>
  <read_first>
    - backend/app/tasks/dte.py (_process_emit completo líneas 51-78, _sync_dte_estado completo líneas 27-48)
    - backend/app/services/boleta_stock.py (NO importar — referencia de qué NO hacer)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "tasks/dte.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md "Pitfall 2" y "Pitfall 5", "Pattern 4", "Pattern 5"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-12, D-16
  </read_first>
  <behavior>
    - _process_emit con emision.guia_despacho_id != None → llama svc.build_guia_payload(guia, db) y obtiene track_id
    - _sync_dte_estado con emision.guia_despacho_id, estado="rechazada" → guia.dte_estado="rechazada" pero NO crea MovimientoInventario
    - _sync_dte_estado con emision.nota_credito_id donde nc.guia_despacho_id != None y estado="aceptada" → guia.estado="anulada"
    - _sync_dte_estado con emision.nota_credito_id donde nc.guia_despacho_id != None y estado="procesando" → guia.estado permanece "emitida" (NO se anula prematuramente)
  </behavior>
  <action>
**A) Agregar import** al tope del archivo (junto a otros models):

```python
from app.models.guia_despacho import GuiaDespacho
```

**B) `_process_emit`** — localizar la cadena `if emision.factura_id: ... elif emision.boleta_id:` y agregar **al final**, antes del `else` final (si existe) o como nueva rama:

```python
elif emision.guia_despacho_id:
    doc = (
        db.query(GuiaDespacho)
        .options(
            joinedload(GuiaDespacho.lineas),
            joinedload(GuiaDespacho.cliente),
        )
        .filter_by(id=emision.guia_despacho_id)
        .first()
    )
    payload = svc.build_guia_payload(doc, db)
```

(`joinedload` debe estar ya importado en el archivo; si no, agregar `from sqlalchemy.orm import joinedload`.)

**C) `_sync_dte_estado`** — dos cambios:

**C.1) Branch nuevo para guía** (insertar después del branch `elif emision.boleta_id:`):

```python
elif emision.guia_despacho_id:
    guia = db.get(GuiaDespacho, emision.guia_despacho_id)
    if guia:
        guia.dte_estado = estado
        # D-12 / D-13: Guía DTE 52 NO descuenta stock — NO llamar revertir_stock_boleta
        # cuando estado == "rechazada" (a diferencia de boleta).
```

**C.2) Modificar el branch existente `elif emision.nota_credito_id:`** para que cuando NC se acepta y tiene `guia_despacho_id`, marque la guía como anulada (D-16). Localizar el bloque:

```python
elif emision.nota_credito_id:
    nc = db.get(NotaCredito, emision.nota_credito_id)
    if nc:
        nc.dte_estado = estado
```

Y extenderlo con la lógica de anulación. El resultado debe quedar:

```python
elif emision.nota_credito_id:
    nc = db.get(NotaCredito, emision.nota_credito_id)
    if nc:
        nc.dte_estado = estado
        # D-16: solo NC aceptada por SII anula legalmente la guía
        if estado == "aceptada" and nc.guia_despacho_id:
            guia = db.get(GuiaDespacho, nc.guia_despacho_id)
            if guia:
                guia.estado = "anulada"
```

LANDMINES críticos (NO violar):
- NUNCA llamar `revertir_stock_boleta` ni `descontar_stock_*` en el branch de guía (Pitfall 2). El test `test_guia_no_descuenta_stock` (Plan 05) lo verificará.
- La anulación de guía SOLO ocurre cuando `estado == "aceptada"`. NO con "procesando" ni "pendiente" (Pitfall 5 / D-16).
- NO importar `app.services.boleta_stock` en el archivo (si ya está, NO usarlo en el branch de guía).
- NO mover el branch de boleta — solo agregar el de guía DESPUÉS y modificar el de NC.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
import inspect
from app.tasks.dte import _sync_dte_estado, _process_emit
src_sync = inspect.getsource(_sync_dte_estado)
src_proc = inspect.getsource(_process_emit)

# guía branch en _sync
assert 'elif emision.guia_despacho_id' in src_sync, src_sync
# guía branch en _process
assert 'elif emision.guia_despacho_id' in src_proc, src_proc
assert 'build_guia_payload' in src_proc

# NC anula guía en _sync
assert 'estado == \"aceptada\"' in src_sync or \"estado == 'aceptada'\" in src_sync
assert 'nc.guia_despacho_id' in src_sync
assert 'estado = \"anulada\"' in src_sync or \"estado = 'anulada'\" in src_sync

# branch de guía NO debe llamar revertir_stock
import re
guia_branch = re.search(r'elif emision\\.guia_despacho_id.*?(?=elif |else:|$)', src_sync, re.DOTALL)
assert guia_branch, 'guia branch no encontrado'
assert 'revertir_stock' not in guia_branch.group(0), 'guia branch llama revertir_stock!'
assert 'descontar_stock' not in guia_branch.group(0)
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "elif emision.guia_despacho_id" backend/app/tasks/dte.py` retorna ≥ 2 matches (uno en _process_emit, uno en _sync_dte_estado)
    - `grep "build_guia_payload" backend/app/tasks/dte.py` retorna ≥ 1 match
    - `grep "nc.guia_despacho_id" backend/app/tasks/dte.py` retorna ≥ 1 match (anulación NC→guía)
    - El bloque python del verify imprime `OK` (verifica que el branch de guía NO contiene `revertir_stock` ni `descontar_stock`)
    - `grep -A 6 "elif emision.guia_despacho_id" backend/app/tasks/dte.py | grep -E "revertir_stock|descontar_stock"` retorna 0 matches
  </acceptance_criteria>
  <done>
    Pipeline polimórfico para tipo "052" extendido en _process_emit y _sync_dte_estado; NC aceptada con guia_despacho_id anula la guía; sin lógica de stock en branch de guía.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Lioren v1 (httpx) | Saliente; auth via API key; respuesta no confiable hasta validar firma |
| Lioren → /api/dte/webhook | Entrante; HMAC validado por svc.validate_webhook_signature (existente) |
| Celery worker → DB | Confiable (mismo proceso); rollback en excepción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Webhook payload alteration | mitigate | HMAC validation `svc.validate_webhook_signature` existente — out of scope para extender |
| T-03-02 | Elevation | Anular guía sin NC aceptada (D-16) | mitigate | Guard `if estado == "aceptada" and nc.guia_despacho_id` en _sync_dte_estado; NC pendiente NO anula |
| T-03-03 | Tampering | Pipeline branch llama stock por error | mitigate | Test test_guia_no_descuenta_stock (Plan 05) y acceptance criteria de Task 3 (grep negativo) |
| T-03-04 | Information Disclosure | Lioren payload expone datos cliente | accept | Cliente RUT/nombre/dirección requeridos por SII; saliente HTTPS |
| T-03-05 | Repudiation | Emisión sin auditoría | mitigate | DteEmision creado dentro de sesión SA; AuditContextMiddleware lo registra |
| T-03-06 | DoS | Lioren downtime bloquea emisión | accept | Celery retry/backoff existente; emisión asíncrona |
</threat_model>

<verification>
- App importable tras edits (script Task 2)
- Branch de guía no contiene stock logic (script verify Task 3)
- build_guia_payload retorna estructura correcta (script verify Task 1)
</verification>

<success_criteria>
- [ ] DteService.build_guia_payload existe y produce dict con tipo_dte=52, ind_traslado, destino
- [ ] POST /api/dte/guias-despacho/{id}/emitir registrado (404 si guía no existe)
- [ ] _process_emit reconoce guia_despacho_id y llama build_guia_payload
- [ ] _sync_dte_estado actualiza guia.dte_estado sin tocar stock
- [ ] _sync_dte_estado en branch NC: si estado=aceptada y nc.guia_despacho_id, marca guia.estado='anulada'
- [ ] Branch de guía no contiene revertir_stock_boleta ni descontar_stock_*
- [ ] TODO sandbox marker presente en build_guia_payload
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-gu-a-de-despacho-52-backend/01-03-SUMMARY.md`.
</output>
