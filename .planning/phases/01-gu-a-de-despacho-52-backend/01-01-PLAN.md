---
phase: 01-gu-a-de-despacho-52-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/guia_despacho.py
  - backend/app/models/dte_emision.py
  - backend/app/models/nota_credito.py
  - backend/app/schemas/guia_despacho.py
  - backend/app/schemas/dte.py
  - backend/migrations/versions/a1b2c3d4e5f6_add_guias_despacho.py
  - backend/tests/conftest.py
autonomous: true
requirements:
  - DTE-01
  - DTE-04
user_setup: []

must_haves:
  truths:
    - "Tablas guias_despacho y guia_despacho_lineas existen con todas las columnas D-02/D-03"
    - "DteEmision tiene columna guia_despacho_id (nullable, FK CASCADE) y check constraint suma 5 FKs == 1"
    - "NotaCredito tiene columna guia_despacho_id (nullable, FK SET NULL)"
    - "system_config seeded con guia_despacho_last_id=0"
    - "GuiaDespachoCreate valida motivo_traslado in {1..9} y lineas no vacías"
    - "NotaCreditoCreate acepta guia_despacho_id opcional"
    - "conftest.py importa app.models.guia_despacho para registrar metadata"
  artifacts:
    - path: "backend/app/models/guia_despacho.py"
      provides: "GuiaDespacho + GuiaDespachoLinea ORM"
      contains: "class GuiaDespacho"
    - path: "backend/app/schemas/guia_despacho.py"
      provides: "Pydantic Create/Update/Out/ListOut"
      contains: "class GuiaDespachoCreate"
    - path: "backend/migrations/versions/a1b2c3d4e5f6_add_guias_despacho.py"
      provides: "Migración Alembic monolítica D-34"
      contains: "def upgrade"
  key_links:
    - from: "backend/app/models/dte_emision.py"
      to: "guias_despacho.id"
      via: "FK guia_despacho_id ON DELETE CASCADE"
      pattern: "ForeignKey.*guias_despacho\\.id"
    - from: "backend/app/models/nota_credito.py"
      to: "guias_despacho.id"
      via: "FK guia_despacho_id ON DELETE SET NULL"
      pattern: "ForeignKey.*guias_despacho\\.id.*SET NULL"
    - from: "ck_dte_emision_one_document"
      to: "5 FKs"
      via: "CHECK constraint suma de CASE WHEN ... = 1"
      pattern: "guia_despacho_id IS NOT NULL"
---

<objective>
Crear modelo SQLAlchemy GuiaDespacho/GuiaDespachoLinea, schemas Pydantic, extender DteEmision y NotaCredito con FK guia_despacho_id, y entregar la migración Alembic monolítica que crea las 2 tablas, agrega las 2 FKs, recrea el check constraint con 5 FKs y siembra el contador de numeración.

Purpose: Establece todos los contratos de datos (modelos, schemas, migración) que los Plans 02/03/04/05 consumirán. Sin esto el resto no compila.

Output: Modelos + schemas + migración aplicables, con tests de conftest pudiendo create_all() la tabla.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md
@.planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

@backend/app/models/boleta.py
@backend/app/models/dte_emision.py
@backend/app/models/nota_credito.py
@backend/app/schemas/boleta.py
@backend/app/schemas/dte.py
@backend/migrations/versions/z5a6b7c8d9e0_add_boletas.py
@backend/tests/conftest.py

<interfaces>
<!-- Contratos clave que ya existen y que este plan consume / extiende -->

From backend/app/models/dte_emision.py (líneas 7-17, check constraint actual con 4 FKs):
```python
class DteEmision(Base):
    __tablename__ = "dte_emisiones"
    __table_args__ = (
        CheckConstraint(
            "(CASE WHEN factura_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN nota_credito_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN nota_debito_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN boleta_id IS NOT NULL THEN 1 ELSE 0 END) = 1",
            name="ck_dte_emision_one_document",
        ),
    )
    tipo: Mapped[str] = mapped_column(String(3))  # "033","039","041","052","056","061"
```

From backend/app/api/dte.py (líneas 25-33, reutilizable):
```python
def _next_numero(db: Session, key: str) -> int:
    cfg = db.query(SystemConfig).filter_by(key=key).with_for_update().first()
    ...
```

From backend/app/schemas/dte.py (NotaCreditoCreate actual):
```python
class NotaCreditoCreate(BaseModel):
    fecha: date | None = None
    cliente_id: int
    razon: str
    lineas: list[NotaCreditoLineaCreate] = []
```

From backend/migrations/versions/z5a6b7c8d9e0_add_boletas.py — patrón dual SQLite/Postgres a copiar (op.batch_alter_table en SQLite, op.add_column + drop+create check constraint en Postgres).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear modelo GuiaDespacho + GuiaDespachoLinea y registrar en conftest</name>
  <files>backend/app/models/guia_despacho.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/app/models/boleta.py (analog exacto, líneas 1-92)
    - backend/tests/conftest.py (línea 77 — import de app.models.boleta)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "backend/app/models/guia_despacho.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-01, D-02, D-03
  </read_first>
  <action>
Crear `backend/app/models/guia_despacho.py` siguiendo el shape de boleta.py (ver PATTERNS.md líneas 36-129). Implementa por D-01/D-02/D-03:

```python
from datetime import date, datetime, timezone
from decimal import Decimal
from sqlalchemy import Boolean, Date, DateTime, ForeignKey, Integer, Numeric, String, Text, text
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base


class GuiaDespacho(Base):
    __tablename__ = "guias_despacho"

    id: Mapped[int] = mapped_column(primary_key=True)
    numero: Mapped[int] = mapped_column(Integer, unique=True, index=True)
    fecha: Mapped[date] = mapped_column(Date, default=date.today)
    motivo_traslado: Mapped[int] = mapped_column(Integer)  # 1..9 (D-05)
    direccion_destino: Mapped[str | None] = mapped_column(String(255), nullable=True)
    comuna_destino: Mapped[str | None] = mapped_column(String(100), nullable=True)
    nota_venta_id: Mapped[int | None] = mapped_column(
        ForeignKey("nota_ventas.id", ondelete="SET NULL"), nullable=True
    )
    cliente_id: Mapped[int | None] = mapped_column(
        ForeignKey("clientes.id", ondelete="SET NULL"), nullable=True, index=True
    )
    empresa_id: Mapped[int | None] = mapped_column(
        ForeignKey("empresas.id", ondelete="SET NULL"), nullable=True
    )
    email_envio: Mapped[str | None] = mapped_column(String(255), nullable=True)
    vendedor_id: Mapped[int | None] = mapped_column(
        ForeignKey("users.id", ondelete="SET NULL"), nullable=True
    )
    estado: Mapped[str] = mapped_column(
        String(20), default="emitida", server_default=text("'emitida'")
    )
    dte_estado: Mapped[str] = mapped_column(
        String(20), default="no_emitida", server_default=text("'no_emitida'"), index=True
    )
    xml_raw: Mapped[str | None] = mapped_column(Text, nullable=True)
    track_id: Mapped[str | None] = mapped_column(String(100), nullable=True, index=True)
    folio_sii: Mapped[int | None] = mapped_column(Integer, nullable=True)
    email_enviado_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    total_neto: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))
    total_iva: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))
    total: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        default=lambda: datetime.now(timezone.utc),
        server_default=text("CURRENT_TIMESTAMP"),
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        default=lambda: datetime.now(timezone.utc),
        onupdate=lambda: datetime.now(timezone.utc),
        server_default=text("CURRENT_TIMESTAMP"),
    )

    cliente: Mapped["Cliente | None"] = relationship("Cliente")
    empresa: Mapped["Empresa | None"] = relationship("Empresa")
    vendedor: Mapped["User | None"] = relationship("User")
    lineas: Mapped[list["GuiaDespachoLinea"]] = relationship(
        "GuiaDespachoLinea",
        back_populates="guia_despacho",
        cascade="all, delete-orphan",
        order_by="GuiaDespachoLinea.orden",
    )
    emision: Mapped["DteEmision | None"] = relationship(
        "DteEmision",
        primaryjoin="DteEmision.guia_despacho_id == GuiaDespacho.id",
        foreign_keys="DteEmision.guia_despacho_id",
        uselist=False,
    )

    @property
    def is_locked(self) -> bool:
        return self.estado != "emitida"


class GuiaDespachoLinea(Base):
    __tablename__ = "guia_despacho_lineas"

    id: Mapped[int] = mapped_column(primary_key=True)
    guia_despacho_id: Mapped[int] = mapped_column(
        ForeignKey("guias_despacho.id", ondelete="CASCADE"), index=True
    )
    orden: Mapped[int] = mapped_column(Integer, default=0)
    producto_id: Mapped[int | None] = mapped_column(
        ForeignKey("productos.id", ondelete="SET NULL"), nullable=True
    )
    descripcion: Mapped[str] = mapped_column(String(500))
    cantidad: Mapped[Decimal] = mapped_column(Numeric(10, 2), default=Decimal("1"))
    precio_unitario: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))
    descuento_pct: Mapped[Decimal] = mapped_column(Numeric(5, 2), default=Decimal("0"))
    exenta: Mapped[bool] = mapped_column(Boolean, default=False, server_default=text("false"))
    total_neto: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))
    iva: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))
    total_linea: Mapped[Decimal] = mapped_column(Numeric(12, 2), default=Decimal("0"))

    guia_despacho: Mapped["GuiaDespacho"] = relationship("GuiaDespacho", back_populates="lineas")
    producto: Mapped["Producto | None"] = relationship("Producto")
```

Luego, en `backend/tests/conftest.py`, ubicar la línea `import app.models.boleta` (≈línea 77) y agregar inmediatamente después:

```python
import app.models.guia_despacho  # noqa: F401 — registers GuiaDespacho with Base.metadata
```

NO eliminar `tipo_dte`, `metodo_pago`, etc., en boleta — solo creamos guía. NO copiar `descontar_stock_*`. NO añadir `patente_vehiculo`/`rut_chofer` (Pitfall 4 / A4 — si Lioren los exige, se agregarán en follow-up).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "from app.models.guia_despacho import GuiaDespacho, GuiaDespachoLinea; assert GuiaDespacho.__tablename__ == 'guias_despacho'; assert GuiaDespachoLinea.__tablename__ == 'guia_despacho_lineas'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class GuiaDespacho" backend/app/models/guia_despacho.py` ≥ 2 (clase GuiaDespacho y GuiaDespachoLinea)
    - `grep "motivo_traslado" backend/app/models/guia_despacho.py` retorna línea con `mapped_column(Integer)`
    - `grep "import app.models.guia_despacho" backend/tests/conftest.py` retorna 1 match
    - `grep -E "descontar_stock|revertir_stock" backend/app/models/guia_despacho.py` retorna 0 matches (D-13)
    - Comando python del verify imprime `OK` sin error
  </acceptance_criteria>
  <done>
    Modelos importables, conftest registra metadata, ningún rastro de stock logic.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extender DteEmision (FK + check 5 FKs) y NotaCredito (FK SET NULL)</name>
  <files>backend/app/models/dte_emision.py, backend/app/models/nota_credito.py</files>
  <read_first>
    - backend/app/models/dte_emision.py (líneas 1-30 — check constraint actual)
    - backend/app/models/nota_credito.py (líneas 1-50 — patrón boleta_id)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "models/dte_emision.py" y "models/nota_credito.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md "Pitfall 3" (no olvidar actualizar el modelo Python además de la migración)
  </read_first>
  <action>
**A) `backend/app/models/dte_emision.py`** — actualizar el bloque `__table_args__` (D-09). El check constraint actual lista 4 FKs; debe listar 5:

```python
__table_args__ = (
    CheckConstraint(
        "(CASE WHEN factura_id IS NOT NULL THEN 1 ELSE 0 END) + "
        "(CASE WHEN nota_credito_id IS NOT NULL THEN 1 ELSE 0 END) + "
        "(CASE WHEN nota_debito_id IS NOT NULL THEN 1 ELSE 0 END) + "
        "(CASE WHEN boleta_id IS NOT NULL THEN 1 ELSE 0 END) + "
        "(CASE WHEN guia_despacho_id IS NOT NULL THEN 1 ELSE 0 END) = 1",
        name="ck_dte_emision_one_document",
    ),
)
```

Y agregar la columna nueva donde están las otras FK polimórficas (después de `boleta_id`):

```python
guia_despacho_id: Mapped[int | None] = mapped_column(
    ForeignKey("guias_despacho.id", ondelete="CASCADE"), nullable=True, index=True
)
```

**B) `backend/app/models/nota_credito.py`** — agregar columna después de `boleta_id` (D-14):

```python
guia_despacho_id: Mapped[int | None] = mapped_column(
    ForeignKey("guias_despacho.id", ondelete="SET NULL"), nullable=True, index=True
)
```

NO cambiar el comportamiento existente. NO tocar `factura_id`, `boleta_id`. Las NC existentes quedarán con `guia_despacho_id = NULL`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "from app.models.dte_emision import DteEmision; from app.models.nota_credito import NotaCredito; cols_dte = {c.name for c in DteEmision.__table__.columns}; cols_nc = {c.name for c in NotaCredito.__table__.columns}; assert 'guia_despacho_id' in cols_dte, cols_dte; assert 'guia_despacho_id' in cols_nc, cols_nc; ck = [c for c in DteEmision.__table__.constraints if getattr(c, 'name', None) == 'ck_dte_emision_one_document'][0]; assert 'guia_despacho_id' in str(ck.sqltext), str(ck.sqltext); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "guia_despacho_id" backend/app/models/dte_emision.py` ≥ 2 (constraint + columna)
    - `grep "guia_despacho_id" backend/app/models/nota_credito.py` retorna línea con `ForeignKey("guias_despacho.id", ondelete="SET NULL")`
    - El comando python del verify imprime `OK` (la nueva expresión del check constraint contiene `guia_despacho_id`)
    - `grep "guia_despacho_id IS NOT NULL" backend/app/models/dte_emision.py` retorna 1 match
  </acceptance_criteria>
  <done>
    DteEmision tiene 5 FKs en check constraint y columna `guia_despacho_id`; NotaCredito tiene FK SET NULL a guías.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schemas Pydantic GuiaDespacho* y extender NotaCreditoCreate</name>
  <files>backend/app/schemas/guia_despacho.py, backend/app/schemas/dte.py</files>
  <read_first>
    - backend/app/schemas/boleta.py (analog completo)
    - backend/app/schemas/dte.py (NotaCreditoCreate actual, líneas ~10-30)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "schemas/guia_despacho.py"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-CONTEXT.md D-04, D-05, D-06, D-15, D-32, D-33
  </read_first>
  <behavior>
    - GuiaDespachoCreate(motivo_traslado=10) → ValidationError ("Input should be 1, 2, 3, 4, 5, 6, 7, 8 or 9")
    - GuiaDespachoCreate(motivo_traslado=1, lineas=[]) → ValidationError ("Guía requiere al menos una línea")
    - GuiaDespachoCreate(motivo_traslado=1, lineas=[{descripcion:"x", cantidad:1, precio_unitario:100}]) → ok
    - NotaCreditoCreate(cliente_id=1, razon="x", guia_despacho_id=5) → ok (campo opcional)
    - NotaCreditoCreate sin guia_despacho_id → ok (default None)
  </behavior>
  <action>
**A) Crear `backend/app/schemas/guia_despacho.py`** copiando shape de boleta.py:

```python
from datetime import date, datetime
from decimal import Decimal
from typing import Literal
from pydantic import BaseModel, ConfigDict, field_validator


class GuiaDespachoLineaCreate(BaseModel):
    orden: int = 0
    producto_id: int | None = None
    descripcion: str
    cantidad: Decimal = Decimal("1")
    precio_unitario: Decimal = Decimal("0")
    descuento_pct: Decimal = Decimal("0")
    exenta: bool = False


class GuiaDespachoLineaOut(GuiaDespachoLineaCreate):
    id: int
    total_neto: Decimal
    iva: Decimal
    total_linea: Decimal
    model_config = ConfigDict(from_attributes=True)


class GuiaDespachoCreate(BaseModel):
    fecha: date | None = None
    motivo_traslado: Literal[1, 2, 3, 4, 5, 6, 7, 8, 9]  # D-05
    direccion_destino: str | None = None
    comuna_destino: str | None = None
    cliente_id: int | None = None
    empresa_id: int | None = None
    nota_venta_id: int | None = None  # D-04: opcional, no bloquea NV
    email_envio: str | None = None
    lineas: list[GuiaDespachoLineaCreate]

    @field_validator("lineas")
    @classmethod
    def lineas_no_vacias(cls, v):
        if not v:
            raise ValueError("Guía requiere al menos una línea")
        return v


class GuiaDespachoUpdate(BaseModel):
    """D-06: solo metadata accesoria."""
    direccion_destino: str | None = None
    comuna_destino: str | None = None
    email_envio: str | None = None


class ClienteMinOut(BaseModel):
    id: int
    nombre: str
    rut: str | None = None
    model_config = ConfigDict(from_attributes=True)


class VendedorMinOut(BaseModel):
    id: int
    username: str
    model_config = ConfigDict(from_attributes=True)


class GuiaDespachoOut(BaseModel):
    id: int
    numero: int
    fecha: date
    motivo_traslado: int
    direccion_destino: str | None
    comuna_destino: str | None
    cliente_id: int | None
    empresa_id: int | None
    nota_venta_id: int | None
    email_envio: str | None
    vendedor_id: int | None
    estado: str
    dte_estado: str
    track_id: str | None
    folio_sii: int | None
    total_neto: Decimal
    total_iva: Decimal
    total: Decimal
    email_enviado_at: datetime | None
    created_at: datetime
    updated_at: datetime
    cliente: ClienteMinOut | None = None
    vendedor: VendedorMinOut | None = None
    lineas: list[GuiaDespachoLineaOut] = []
    model_config = ConfigDict(from_attributes=True)


class GuiaDespachoListOut(BaseModel):
    """D-33: lista sin líneas para performance."""
    id: int
    numero: int
    fecha: date
    cliente_id: int | None
    motivo_traslado: int
    total: Decimal
    estado: str
    dte_estado: str
    track_id: str | None
    model_config = ConfigDict(from_attributes=True)


class GuiaEmailBody(BaseModel):
    email: str | None = None
```

**B) Modificar `backend/app/schemas/dte.py`** — encontrar `NotaCreditoCreate` y agregar el campo opcional (D-15):

```python
class NotaCreditoCreate(BaseModel):
    fecha: date | None = None
    cliente_id: int
    razon: str
    lineas: list[NotaCreditoLineaCreate] = []
    guia_despacho_id: int | None = None  # D-15: NC puede anular guía
```

NO agregar `model_validator` XOR todavía (Pitfall 6 / D-15: el schema actual no expone `factura_id`, así que el XOR aplicará en futuro). Solo agregar el campo opcional.

NO usar `Literal["39","41"]` para motivo (eso es de boleta tipo_dte). Aquí es `Literal[1..9]` Integer.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "
from pydantic import ValidationError
from app.schemas.guia_despacho import GuiaDespachoCreate, GuiaDespachoLineaCreate
from app.schemas.dte import NotaCreditoCreate

# motivo invalido
try:
    GuiaDespachoCreate(motivo_traslado=10, lineas=[GuiaDespachoLineaCreate(descripcion='x')])
    raise AssertionError('motivo=10 should fail')
except ValidationError:
    pass

# lineas vacias
try:
    GuiaDespachoCreate(motivo_traslado=1, lineas=[])
    raise AssertionError('empty lineas should fail')
except ValidationError:
    pass

# happy
g = GuiaDespachoCreate(motivo_traslado=1, lineas=[GuiaDespachoLineaCreate(descripcion='x')])
assert g.motivo_traslado == 1

# NC con guia_despacho_id
nc = NotaCreditoCreate(cliente_id=1, razon='r', guia_despacho_id=5)
assert nc.guia_despacho_id == 5
nc2 = NotaCreditoCreate(cliente_id=1, razon='r')
assert nc2.guia_despacho_id is None
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Literal\[1, 2, 3, 4, 5, 6, 7, 8, 9\]" backend/app/schemas/guia_despacho.py` retorna 1 match
    - `grep "lineas_no_vacias" backend/app/schemas/guia_despacho.py` retorna ≥ 1 match
    - `grep "guia_despacho_id: int | None" backend/app/schemas/dte.py` retorna ≥ 1 match
    - `grep "GuiaDespachoListOut" backend/app/schemas/guia_despacho.py` retorna ≥ 1 match
    - El bloque python del verify imprime `OK`
  </acceptance_criteria>
  <done>
    Schema valida motivo 1..9 y líneas no vacías; NotaCreditoCreate acepta `guia_despacho_id` opcional.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Migración Alembic monolítica add_guias_despacho</name>
  <files>backend/migrations/versions/a1b2c3d4e5f6_add_guias_despacho.py</files>
  <read_first>
    - backend/migrations/versions/z5a6b7c8d9e0_add_boletas.py (template completo, líneas 1-194)
    - backend/migrations/versions/ (listar para confirmar `down_revision` correcto — usar la cabeza actual)
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-PATTERNS.md sección "migrations/versions/"
    - .planning/phases/01-gu-a-de-despacho-52-backend/01-RESEARCH.md sección "Migración Alembic Monolítica"
  </read_first>
  <action>
Antes de escribir, ejecutar `cd backend &amp;&amp; alembic heads` para identificar la revisión cabeza actual y úsala como `down_revision`. Si la salida es `z5a6b7c8d9e0`, úsala; si hay otra más reciente (ej. una migración posterior a boletas), úsala en su lugar.

Crear el archivo `backend/migrations/versions/a1b2c3d4e5f6_add_guias_despacho.py` con este shape (D-34 / D-35):

```python
"""add_guias_despacho

Revision ID: a1b2c3d4e5f6
Revises: <heads-actual>
Create Date: 2026-04-26

W1-05: tablas guias_despacho + guia_despacho_lineas, FK guia_despacho_id en
dte_emisiones (drop+recreate ck_dte_emision_one_document con 5 FKs) y notas_credito,
seed system_config('guia_despacho_last_id', '0').

Reversible — guías y NCs vinculadas se pierden si downgrade tras emisión real.
"""
from alembic import op
import sqlalchemy as sa


revision = "a1b2c3d4e5f6"
down_revision = "<heads-actual>"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # 1) guias_despacho
    op.create_table(
        "guias_despacho",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("numero", sa.Integer(), nullable=False),
        sa.Column("fecha", sa.Date(), nullable=False),
        sa.Column("motivo_traslado", sa.Integer(), nullable=False),
        sa.Column("direccion_destino", sa.String(255), nullable=True),
        sa.Column("comuna_destino", sa.String(100), nullable=True),
        sa.Column("cliente_id", sa.Integer(), sa.ForeignKey("clientes.id", ondelete="SET NULL"), nullable=True),
        sa.Column("empresa_id", sa.Integer(), sa.ForeignKey("empresas.id", ondelete="SET NULL"), nullable=True),
        sa.Column("nota_venta_id", sa.Integer(), sa.ForeignKey("nota_ventas.id", ondelete="SET NULL"), nullable=True),
        sa.Column("email_envio", sa.String(255), nullable=True),
        sa.Column("vendedor_id", sa.Integer(), sa.ForeignKey("users.id", ondelete="SET NULL"), nullable=True),
        sa.Column("total_neto", sa.Numeric(12, 2), nullable=False, server_default="0"),
        sa.Column("total_iva", sa.Numeric(12, 2), nullable=False, server_default="0"),
        sa.Column("total", sa.Numeric(12, 2), nullable=False, server_default="0"),
        sa.Column("estado", sa.String(20), nullable=False, server_default="emitida"),
        sa.Column("dte_estado", sa.String(20), nullable=False, server_default="no_emitida"),
        sa.Column("xml_raw", sa.Text(), nullable=True),
        sa.Column("track_id", sa.String(100), nullable=True),
        sa.Column("folio_sii", sa.Integer(), nullable=True),
        sa.Column("email_enviado_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.text("CURRENT_TIMESTAMP")),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.text("CURRENT_TIMESTAMP")),
    )
    op.create_index("ix_guias_despacho_numero", "guias_despacho", ["numero"], unique=True)
    op.create_index("ix_guias_despacho_fecha", "guias_despacho", ["fecha"])
    op.create_index("ix_guias_despacho_cliente_id", "guias_despacho", ["cliente_id"])
    op.create_index("ix_guias_despacho_dte_estado", "guias_despacho", ["dte_estado"])
    op.create_index("ix_guias_despacho_track_id", "guias_despacho", ["track_id"])

    # 2) guia_despacho_lineas
    op.create_table(
        "guia_despacho_lineas",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("guia_despacho_id", sa.Integer(), sa.ForeignKey("guias_despacho.id", ondelete="CASCADE"), nullable=False),
        sa.Column("orden", sa.Integer(), nullable=False, server_default="0"),
        sa.Column("producto_id", sa.Integer(), sa.ForeignKey("productos.id", ondelete="SET NULL"), nullable=True),
        sa.Column("descripcion", sa.String(500), nullable=False),
        sa.Column("cantidad", sa.Numeric(10, 2), nullable=False, server_default="1"),
        sa.Column("precio_unitario", sa.Numeric(12, 2), nullable=False, server_default="0"),
        sa.Column("descuento_pct", sa.Numeric(5, 2), nullable=False, server_default="0"),
        sa.Column("exenta", sa.Boolean(), nullable=False, server_default=sa.text("false")),
        sa.Column("total_neto", sa.Numeric(12, 2), nullable=False, server_default="0"),
        sa.Column("iva", sa.Numeric(12, 2), nullable=False, server_default="0"),
        sa.Column("total_linea", sa.Numeric(12, 2), nullable=False, server_default="0"),
    )
    op.create_index("ix_guia_despacho_lineas_guia_id", "guia_despacho_lineas", ["guia_despacho_id"])

    bind = op.get_bind()

    # 3) FK + check constraint en dte_emisiones (5 FKs)
    if bind.dialect.name == "postgresql":
        op.add_column("dte_emisiones", sa.Column("guia_despacho_id", sa.Integer(), nullable=True))
        op.create_foreign_key(
            "fk_dte_emisiones_guia_despacho_id", "dte_emisiones", "guias_despacho",
            ["guia_despacho_id"], ["id"], ondelete="CASCADE",
        )
        op.create_index("ix_dte_emisiones_guia_despacho_id", "dte_emisiones", ["guia_despacho_id"])
        result = bind.execute(sa.text(
            "SELECT 1 FROM pg_constraint WHERE conname = 'ck_dte_emision_one_document'"
        )).fetchone()
        if result:
            op.drop_constraint("ck_dte_emision_one_document", "dte_emisiones", type_="check")
        op.create_check_constraint(
            "ck_dte_emision_one_document",
            "dte_emisiones",
            "(CASE WHEN factura_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN nota_credito_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN nota_debito_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN boleta_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN guia_despacho_id IS NOT NULL THEN 1 ELSE 0 END) = 1",
        )
    else:
        with op.batch_alter_table("dte_emisiones") as batch_op:
            batch_op.add_column(sa.Column("guia_despacho_id", sa.Integer(), nullable=True))

    # 4) FK en notas_credito
    if bind.dialect.name == "postgresql":
        op.add_column("notas_credito", sa.Column("guia_despacho_id", sa.Integer(), nullable=True))
        op.create_foreign_key(
            "fk_notas_credito_guia_despacho_id", "notas_credito", "guias_despacho",
            ["guia_despacho_id"], ["id"], ondelete="SET NULL",
        )
        op.create_index("ix_notas_credito_guia_despacho_id", "notas_credito", ["guia_despacho_id"])
    else:
        with op.batch_alter_table("notas_credito") as batch_op:
            batch_op.add_column(sa.Column("guia_despacho_id", sa.Integer(), nullable=True))

    # 5) seed system_config
    if bind.dialect.name == "postgresql":
        op.execute(
            "INSERT INTO system_config (key, value) VALUES ('guia_despacho_last_id', '0') "
            "ON CONFLICT (key) DO NOTHING"
        )
    else:
        op.execute(
            "INSERT OR IGNORE INTO system_config (key, value) VALUES ('guia_despacho_last_id', '0')"
        )


def downgrade() -> None:
    bind = op.get_bind()

    op.execute("DELETE FROM system_config WHERE key = 'guia_despacho_last_id'")

    if bind.dialect.name == "postgresql":
        op.drop_index("ix_notas_credito_guia_despacho_id", table_name="notas_credito")
        op.drop_constraint("fk_notas_credito_guia_despacho_id", "notas_credito", type_="foreignkey")
        op.drop_column("notas_credito", "guia_despacho_id")

        op.drop_constraint("ck_dte_emision_one_document", "dte_emisiones", type_="check")
        op.create_check_constraint(
            "ck_dte_emision_one_document",
            "dte_emisiones",
            "(CASE WHEN factura_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN nota_credito_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN nota_debito_id IS NOT NULL THEN 1 ELSE 0 END) + "
            "(CASE WHEN boleta_id IS NOT NULL THEN 1 ELSE 0 END) = 1",
        )
        op.drop_index("ix_dte_emisiones_guia_despacho_id", table_name="dte_emisiones")
        op.drop_constraint("fk_dte_emisiones_guia_despacho_id", "dte_emisiones", type_="foreignkey")
        op.drop_column("dte_emisiones", "guia_despacho_id")
    else:
        with op.batch_alter_table("notas_credito") as batch_op:
            batch_op.drop_column("guia_despacho_id")
        with op.batch_alter_table("dte_emisiones") as batch_op:
            batch_op.drop_column("guia_despacho_id")

    op.drop_index("ix_guia_despacho_lineas_guia_id", table_name="guia_despacho_lineas")
    op.drop_table("guia_despacho_lineas")

    op.drop_index("ix_guias_despacho_track_id", table_name="guias_despacho")
    op.drop_index("ix_guias_despacho_dte_estado", table_name="guias_despacho")
    op.drop_index("ix_guias_despacho_cliente_id", table_name="guias_despacho")
    op.drop_index("ix_guias_despacho_fecha", table_name="guias_despacho")
    op.drop_index("ix_guias_despacho_numero", table_name="guias_despacho")
    op.drop_table("guias_despacho")
```

Reemplazar `<heads-actual>` con el resultado real de `alembic heads`. Si `alembic heads` muestra `z5a6b7c8d9e0 (head)`, usar `down_revision = "z5a6b7c8d9e0"`.

NO ejecutar `alembic upgrade head` en este task — la migración se aplicará al levantar tests en plans posteriores. Solo confirmar que `alembic check` o `alembic upgrade head --sql` no falla.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; alembic upgrade head --sql 2>&amp;1 | grep -E "CREATE TABLE guias_despacho|CREATE TABLE guia_despacho_lineas|guia_despacho_id" | head -5 &amp;&amp; alembic upgrade head &amp;&amp; python -c "from sqlalchemy import inspect; from app.database import engine; ins = inspect(engine); assert 'guias_despacho' in ins.get_table_names(); assert 'guia_despacho_lineas' in ins.get_table_names(); cols = {c['name'] for c in ins.get_columns('dte_emisiones')}; assert 'guia_despacho_id' in cols; cols_nc = {c['name'] for c in ins.get_columns('notas_credito')}; assert 'guia_despacho_id' in cols_nc; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo existe: `ls backend/migrations/versions/*add_guias_despacho*.py` retorna 1 match
    - `grep -c "op.create_table" backend/migrations/versions/*add_guias_despacho*.py` ≥ 2
    - `grep "guia_despacho_id IS NOT NULL" backend/migrations/versions/*add_guias_despacho*.py` retorna ≥ 1 match (check constraint nuevo)
    - `grep "guia_despacho_last_id" backend/migrations/versions/*add_guias_despacho*.py` retorna ≥ 2 matches (seed upgrade + delete downgrade)
    - `cd backend && alembic upgrade head` completa sin error
    - El bloque python del verify imprime `OK` (las dos tablas y ambas FK existen)
  </acceptance_criteria>
  <done>
    Migración aplica sin error en SQLite y produce SQL válido en Postgres dry-run; tablas + FKs + seed presentes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Pydantic schemas) | Datos no confiables atraviesan validación en GuiaDespachoCreate y NotaCreditoCreate |
| migration → DB | DDL ejecutado por proceso autorizado (developer); no atraviesa frontera externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | GuiaDespachoCreate.motivo_traslado | mitigate | `Literal[1..9]` Pydantic validator (D-05) — rechaza valores fuera de rango con 422 |
| T-01-02 | Tampering | GuiaDespachoCreate.lineas | mitigate | `lineas_no_vacias` field_validator → ValueError → 422 |
| T-01-03 | Tampering | DteEmision con múltiples FKs (factura+guía) | mitigate | Check constraint `ck_dte_emision_one_document` con suma == 1 (5 FKs) impide emisión polimórfica inválida |
| T-01-04 | Information Disclosure | NotaCreditoCreate sin XOR factura/guía | accept | El schema actual no expone `factura_id`; XOR se agregará cuando aplique. Pitfall 6 se cubre en Plan 03 vía pipeline. |
| T-01-05 | Tampering | descripcion línea (free text) | mitigate | `String(500)` cap; XSS no aplica (PDF server-side via Jinja2 autoescape en Plan 04) |
| T-01-06 | Repudiation | Cambios en GuiaDespacho sin auditoría | mitigate | Modelo creado con campos `created_at`/`updated_at`; whitelist auditoría se agrega en Plan 02 (D-25) |
</threat_model>

<verification>
- `cd backend && alembic upgrade head` exit 0
- `cd backend && python -m pytest tests/conftest.py --collect-only -q` exit 0 (conftest carga sin error)
- Schema valida motivo 1..9 (script python en verify Task 3)
- Modelos importables (script python Task 1)
</verification>

<success_criteria>
- [ ] guias_despacho + guia_despacho_lineas existen tras `alembic upgrade head`
- [ ] dte_emisiones tiene columna guia_despacho_id y check constraint con 5 FKs
- [ ] notas_credito tiene columna guia_despacho_id (FK SET NULL)
- [ ] system_config tiene fila ('guia_despacho_last_id', '0')
- [ ] GuiaDespachoCreate rechaza motivo=10 con ValidationError
- [ ] NotaCreditoCreate acepta guia_despacho_id opcional
- [ ] conftest.py importa app.models.guia_despacho
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-gu-a-de-despacho-52-backend/01-01-SUMMARY.md`.
</output>
